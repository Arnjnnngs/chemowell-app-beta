# ChemoWell (APP-BETA)

Private medication & symptom tracker for chemo patients and caregivers. **All data stays on the device — no cloud, no accounts, no tracking.**

This is the native-app codebase (APP-BETA in the 4-target system). It was seeded from `chemowell-beta` (WEB-BETA) at v71 and differs in one fundamental way: Firebase/Firestore is fully removed and replaced with an on-device storage layer (`localStorage`), plus a first-run setup flow. It will be wrapped with Capacitor for Play Store / App Store distribution.

- Live test build: https://arnjnnngs.github.io/chemowell-app-beta/
- Sibling repos: `care-tracker` (WEB-MAIN, Brandi's live app) · `chemowell-beta` (WEB-BETA, staging for WEB-MAIN)

## Version history

| Version | Date | Changes |
|---|---|---|
| app-v22 | 2026-07-28 | Owner-requested: the app "still looks basic" — moved to the standard Android app pattern of a hidden hamburger nav drawer (top-left, three-line icon replaces the old standalone "?" and gear header buttons) opening a slide-in panel with an identity header (active profile name, tap → Settings) and two destinations: **Calendar** and **Settings**. **Calendar** is a new view for tracking future appointments (title, date via the app's existing calendar-grid picker, time, optional note, optional reminder: 1 hour before / morning-of / 1 day before / none) — reminders fire through the app's existing native/web notification path via a new, independent check that can't be taken down by a bug in the medication-reminder system. Upcoming appointments list ascending with a collapsible Past section. **FAQ** — an 8-question accordion added to Settings (privacy, adding meds, switching profiles, missed doses, treatment date, the weight-reason field, Calendar reminders, starting over); medical-advice questions stay explicitly out of scope next to the existing disclaimer. **Replay the walkthrough** confirmed still in Settings from a prior release; the tour's final-step copy updated since it referenced the now-removed header "?" button. **Steroid weight-reason suggestion** (carried over from the app-v21 research report rather than left as a future candidate): the weight-log modal now shows a tap-to-fill hint when an active, non-paused medication name-matches a common oncology steroid (Dexamethasone, Prednisone, etc.) and no reason is picked yet — never auto-selects, just offers. Researched Android nav-drawer conventions (Material Design 3 spec: width, scrim opacity, animation timing, touch targets, focus trap) before building, and flagged to the Owner that current Google guidance actually leans away from combining a drawer with a 5-tab bottom nav on phone screens — built per explicit request anyway, executed to spec. Full chain: Designer found 2 should-fix issues (3 new touch targets under the app's 44px floor; steroid-suggestion and calendar-reminder icons using raw color emoji instead of the app's custom SVG line-icon language) — both fixed. Auditor found 1 blocker (a fast double-tap on the hamburger could land its second tap on the drawer's identity button underneath, force-navigating to Settings — fixed by moving the close button to its own top-right control so the identity button no longer occupies the hamburger's former screen position, re-verified with a real two-click test at the hamburger's exact coordinates), plus 3 should-fix (a "morning-of" reminder for an appointment before 9 AM could fire after the appointment already happened — fixed with an automatic 1-hour-before fallback; editing any field on an already-reminded appointment, even just a note, silently re-armed its reminder — fixed to only reset on an actual date/time/reminder-choice change; nested-button invalid markup on the old close control — fixed as part of the blocker fix) and 1 nice-to-have (opening the drawer mid-tour buried the tour card with no way back — fixed by having the hamburger no-op while the tour is active). All findings re-verified against their specific repro, zero regressions on the full v21 weight/paracentesis regression suite (mobile + desktop, zero console errors both). |
| app-v21 | 2026-07-27 | Owner-requested: document *why* weight changed, not just the number — critical for this patient's Paracentesis fluid drains, which cause a real, fast weight drop that looked alarming in the trend chart with no context attached. Weight logging now has an optional **Reason for change** dropdown (Paracentesis, Fluid retention/swelling, Poor appetite, Nausea/vomiting/diarrhea, Steroid medication, Illness/infection, Eating more, Other), a **Liters drained** field that only appears when Paracentesis is selected, and a free-text **Note**. All three show in the Weight report's reading list (reason + liters under each entry, note italicized beneath) and in the compact History label. New fields (`weightReason`/`litersDrained`/`weightNote`) are additive to the existing weight entry — no migration needed, older entries render exactly as before with the new lines simply omitted. Researched clinically what's worth documenting for chemo/radiation weight changes (ascites/paracentesis nursing guidance, NCI weight-change guidance, cancer cachexia/steroid literature) before building; see Owner report for what else surfaced as future candidates. Full chain: Designer review caught a real bug before ship — the Paracentesis label ("Paracentesis (fluid drained)") hard-clipped mid-character in the reason dropdown on both mobile and desktop with no ellipsis, so once selected the user couldn't reread their own choice — fixed with `text-overflow:ellipsis` on the select. Auditor caught two real defects: liters values outside 0–20 (negative, zero, over 20) were silently dropped at save time with an identical-looking success toast, so a caregiver would see "Paracentesis" logged with no liters and no idea why — fixed to block the save with an explicit "Enter a valid liters amount (0–20), or leave it blank" toast, matching the app's existing validation pattern used for weight/temperature/blood-pressure; separately, `liters === 0` (a legitimate value — "attempted drain, negligible fluid") was being rejected by the same bug even though the field itself allows 0 — fixed alongside. Both fixes re-verified live (rejected: -5, 0-accepted, 25-rejected, blank-accepted; ellipsis rendering confirmed via screenshot) with zero regressions on the full happy-path suite (mobile + desktop, zero console errors). |
| app-v20 | 2026-07-27 | Owner-requested batch of three medication-editor features, shipped together: **(1) Pause a medication** — a manual "Pause"/"Resume" toggle in the editor (edit mode only) to temporarily deactivate any medication for any reason without deleting it. While paused: the medication's card becomes an inert, muted card on Home ("— Paused", no logging), dose logging is fully blocked, missed-dose flags stop accruing, and a daily Home-only check-in banner ("Still pausing {name}?") re-asks once per day until resumed or reconfirmed. Owner explicitly required that resuming must not flood the user with a backlog of "missed dose" flags for the days it was paused — solved by tracking `pausePeriods` (start/end date ranges) rather than gating solely on the live `paused` boolean, so `missedDosesFor()` permanently skips exactly the days a medication was actually paused, whether or not it's still paused now; verified live across real simulated day boundaries (pause → 2 days pass → resume) with zero missed-dose flags appearing for the paused window. **(2) Excluded near treatment day** — the inverse of the existing v16 "Only near treatment day" feature (a medication hidden/unavailable during a configurable window around the treatment date, instead of the reverse). The old single boolean toggle was replaced by a 3-way "Treatment-day availability" radiogroup (Always available / Only near treatment day / Excluded near treatment day), reusing the existing per-medication days-before/after fields so both modes share one exclusive control instead of two independent toggles that could contradict each other. **(3) Schedule-window time picker** — the free-text "Schedule windows" field (e.g. "8 AM-12 PM Morning", prone to silent typos that dropped a window entirely) was replaced with a repeatable list of Start/End-time dropdown rows at 15-minute increments plus an optional per-row label and a live plain-language preview ("Reminds between 8:00 AM and 8:30 AM"); window times now store as decimal hours (`8.25` = 8:15 AM) instead of whole hours only, fixing several landmines found during investigation (`hourTs()` truncated fractional hours, `dueRemindersAt()`'s reminder-firing check only compared whole hours, `normalizeMedication()`'s start-time clamp capped at 23 instead of 23.75) that would otherwise have silently broken quarter-hour reminders. Quality Chain: Designer review found 4 issues (two 40px touch targets under the app's 44px convention, two badges for opposite treatment-day states sharing one color, a paused row in grouped Morning/Afternoon/Evening cards missing the muted styling its standalone counterpart got) — all fixed directly per the Owner's minor-fix amendment. Lead Designer re-verification caught a real regression the touch-target fix itself introduced (the enlarged delete button no longer fit on the same line as the two time selects at 360-390px, silently wrapping instead of failing loudly) — fixed by trimming the row's gap/select minimums to restore the deleted 4px plus margin, confirmed live at both widths with 2+ windows. QA Tester found and got fixed a toast-occlusion bug (the save-confirmation toast blocked taps on the medication editor's Discard/Add buttons at keyboard-open phone heights) — fixed with `pointerEvents:'none'` on the toast, since it never needed to receive a tap. **Chain restart:** the Auditor's first full pass then found two MAJOR (P1) defects that sent this release back through the whole chain per `TEAM.md`'s restart rule: (1) deleting a medication and re-adding one under the same name silently discarded its `pausePeriods`, reviving exactly the "missed dose flood" the pause feature was built to prevent — fixed by archiving `pausePeriods` on delete (closing any still-open period at that moment) and restoring them when a new medication's derived id matches an archived entry, which also surfaced and fixed a second, independent bug where `loadMedicationConfig()` silently discarded the just-archived data on reload whenever the medication list was validly empty; (2) the standalone Home card and the grouped-card row disagreed on whether "paused" or "excluded near treatment day" won when a medication was both — the standalone card vanished entirely instead of showing "Paused" — fixed by moving the exclusion check into the card's per-row rendering so pause always wins, matching every other guard in the file. A fresh Designer/Lead Designer/QA/Auditor pass on the restart caught a third variant of the same ordering bug for the sibling "only near treatment day" mode (paused + only-mode-outside-window also vanished) — fixed with the same pattern. Full chain re-ran clean end to end; Lead Auditor independently reproduced the core scenarios via real UI interaction (not just state injection) before clearing it to the Project Manager gate. One pre-existing, unrelated, non-blocking issue surfaced by QA during the restart pass (the History day-summary count includes non-dose entries like vitals/symptoms) is logged for a future pass, not fixed here. |
| app-v19 | 2026-07-26 | Owner-reported: Zofran/Iron/Compazine cards stayed "Available" right after logging a dose, even though the dose showed up correctly in Today's journal. Root cause confirmed by Aaron checking his own device (no cloud sync, so this couldn't be verified remotely): those medications are "As needed" (`type: 'gap'`) with **Minimum gap hours left at 0** — by the existing design, a 0/blank gap means no cooldown at all, so the card re-opens the instant it's logged. That's not what any real medication needs. Fix (Owner-specified): `saveMedicationEditor()` now rejects saving a gap-timer medication with 0 hours (`type==='gap' && gapH<=0` → toast, save blocked), the "Minimum gap hours" field copy changed from "Leave blank for no minimum gap" to "Required — how long before this can be logged again," and the Meds-tab rule summary for any medication saved before this rule existed now reads "No minimum gap set — edit to fix" instead of "As needed," so Aaron can spot and correct Zofran/Iron/Compazine (and any other pre-existing zero-gap medication) at a glance. Scope: HTML beta only (APP-BETA), per Owner instruction — no native/Capacitor-specific code touched. Existing medications are unaffected until edited (no silent data migration); this only gates future saves. QA: Node syntax check clean; verified new save-blocked toast fires for gap=0/blank and clears once a positive value is entered; verified `formatRuleSummary` warning text renders only for `type:'gap'` meds with `gapH<=0` and leaves scheduled/window meds and meds with a real gap value untouched; verified the Home card lock/unlock math itself (`status()`) was not touched — this is purely a data-entry guardrail, not a change to how gap timers are evaluated. |
| app-v18 | 2026-07-26 | LEAN RUN per TEAM.md Owner amendment ("if there is an issue...it needs to be fixed. just tell me what was fixed afterwards"). Fixes the Reports → Blood Pressure crash QA found (and flagged as pre-existing, out of scope) during the app-v17 walkthrough: `renderBloodPressureReport()` returned a single DOM node in both its empty-state and populated-state branches, unlike every sibling report renderer (`renderHistory`, `renderWeightTrend`, `renderCycle`, `renderAppetite`, `renderBowelMovementReport`, `renderInPatient`), which all return an array. `renderReportDetail()` spreads whichever renderer's result it gets (`...content`) into its own returned array — spreading a bare `Element` isn't valid, so it threw `TypeError: content is not iterable` the instant the tile was tapped, before the new DOM ever replaced the old one, so the tap produced zero visible feedback and the same error then repeated once a second forever via the app's existing 1s tick loop, until the user navigated away. Pure display bug: `logBloodPressure()`/`addEntryDB()` write straight to storage on a code path this bug never touched, so no reading was ever at risk — readings just couldn't be viewed. Fix: wrap both `return` statements in `[...]`, matching the sibling convention exactly; nothing else in the function changed. Combined verification pass (Designer + QA + Auditor in one lean pass, PM checklist run by the Lead Developer, per TEAM.md's lean-mode provision) confirmed: empty state renders with zero console errors across 3+ tick cycles; a real logged reading renders correctly and the Home card's "Last sys/dia" stays in sync; 3 readings render together correctly, sorted newest-first, verified against raw localStorage, not just on-screen text; Remove works mid-list and also removing-to-empty falls back to the empty-state copy cleanly; all 6 report types (including the preference-gated Cycle report) still open with zero regressions; the Home blood-pressure quick-log card, a fully separate code path, is unaffected. Verified at 360×740 and 390×844. One pre-existing, unrelated, non-blocking observation surfaced and logged for the backlog: BP entries logged within the same second can share a truncated timestamp, affecting only the display order of same-second ties, never data loss — not touched by this fix. |
| app-v17 | 2026-07-26 | Owner-reported: the guided-tour banner and its highlighted target blended into the app's own rose accent family and didn't grab attention. Both now get a green (`#2E7D4F`, the app's existing "success/active" token — Plans sheet "Current plan" border, Settings "Active" badge) 3px border/outline with a pulsing glow, replacing the old translucent rose. Pulse uses a phase-synced negative `animation-delay` computed from `Date.now()` on every apply (`tourPulseDelay()`/`applyTourHighlight()`) so a freshly-rebuilt DOM node (the whole app re-renders every 1s tick) resumes the glow mid-cycle instead of restarting it — the same flicker class app-v11 fixed for the Plans sheet, but for a continuous loop instead of a one-shot animation. Design review caught and this pass fixed two real issues before ship: the pulse's contrast floor (0.3 alpha) fell under the WCAG 3:1 minimum against white/card backgrounds for roughly half of every cycle — raised to 0.75; and `prefers-reduced-motion` didn't actually stop the glow — the app's existing global CSS guard neutralizes one-shot animations but not `infinite`-iteration ones, so it was looping at an absurd frequency instead of freezing, a real flicker risk for photosensitive users. Fixed with a JS-level `prefersReducedMotion()` check that skips the animation properties entirely when set, rather than relying on the CSS override. Full chain: Designer + Lead Designer (independent re-verification of both fixes), QA "User Zero" full first-run + daily-loop walkthrough (tour change itself: clean pass, including rapid-tap and double-tap-through-highlight edge cases), Auditor (code audit + 7 live edge-case journeys: double-tap tour restart, navigate-away-mid-tour, tour step-back on discarded med editor, reload-mid-tour, live reduced-motion toggle, viewport resize, missing-target guard — all pass). QA independently found an unrelated, pre-existing bug while exercising the whole product (Reports → Blood Pressure crashes on open, `renderBloodPressureReport()` returns a single node instead of an array like every sibling report) — not part of this release's diff, tracked separately, not fixed here. |
| app-v16 | 2026-07-25 | Two Owner-reported bugs fixed, both real (not UX confusion) — full chain incl. independent Auditor agent. (1) **Custom per-medication treatment-day window**: the "Chemo-day only" toggle previously hid/showed a medication using a hardcoded fixed ±1-day window around the treatment date, inherited from the original single-patient Dexamethasone regimen — inescapable for every medication regardless of that patient's actual regimen. Renamed to "Treatment-day medication" with two new editable fields, Days-before/Days-after treatment (e.g. Aaron's example: 3 days before, 3 days after), defaulting to 1/1 so existing configs behave identically until edited (`treatmentOnly`/`treatmentDaysBefore`/`treatmentDaysAfter` replace `chemoOnly`, migrated in `normalizeMedication`). All "Chemo" wording on the toggle, its badge, and the global treatment-date card/toasts is now "Treatment," matching language already used elsewhere in the app (Home's "Treatment schedule" card, "Treatment day" banner). (2) **Dose reminders now actually work for real medications**: `checkNotifications()` had always fired on a hardcoded clock schedule for five hardcoded legacy medication ids (protonix/iron/compazine/buspirone/paroxetine) — ids `RESERVED_LEGACY_MED_IDS` explicitly forbids any user-created medication from ever having, so every reminder branch was unreachable dead code on every real install; there was no way to get a reminder for a medication actually added to the app. Replaced with a generic `dueRemindersAt()` that reads the same data every scheduled medication already has (its own `windows` + auto-derived `alerts` flag) — the same source of truth the passive missed-dose banner already used — firing once per schedule window per day if the dose isn't logged yet. Independent Auditor agent caught and this pass fixed two real bugs before ship: `clampTreatmentDays('')` was silently saving 0 instead of the documented fallback of 1 (a cleared day-count field would silently narrow a patient's window), and the reminder de-dupe key collided when a medication had two schedule windows starting at the same hour (second window's reminder would never fire). QA: Node harness 35/35 (30 pure-logic incl. DST spring-forward + both audit-caught edge cases, 5 migration-safety), Playwright smoke 360/390/1280px zero console/page errors, full tab sweep clean, `app-v16` confirmed in Settings. |
| app-v15 | 2026-07-24 | Safe-area fix (Aaron's APK bug report): header, guide banner, tour-card clamp, welcome screen, and time-modal overlay now clear the system status bar via a single --safe-top CSS var — env(safe-area-inset-top) when the platform reports it, a 28px literal only when a WebView reports 0 (probe + '; wv)'/isNativeApp detection; replace-not-sum, so no double padding). Zero layout change in desktop browsers. Quality Chain: LEAN RUN per Owner amendment (Developer brief + combined design/QA/audit verification + PM checklist by Lead Dev). |
| app-v14 | 2026-07-24 | Native Android test shell (Owner: "Maximum Effort" — full chain, not lean). Adds a Capacitor project (`android/`, `capacitor.config.ts`, `www/` fallback splash) built for on-device testing of real look/feel + real notifications before anything goes near a store. Deliberately scoped to shell + notifications only — SQLite storage migration and real store billing stay queued (not needed to test feel/notifications, avoided rushing them). Runs in **remote mode**: the native shell loads the live GitHub Pages URL (`server.url` in config) rather than bundling a local copy, so the existing push-via-Chrome workflow keeps updating the app instantly with zero rebuild for content/UI changes — only native-only code (this config, `android/`, the notification bridge below) ever needs a rebuild. New `isNativeApp()` helper (checks `window.Capacitor.isNativePlatform()`, wrapped in try/catch) gates every native-only path so **web/PWA behavior is byte-for-byte unchanged** — proved by re-running the full v14 regression suite with the Capacitor bridge scripts unreachable (this sandbox's network blocks all CDN hosts) and confirming zero errors and identical behavior. Notification system rewired: `checkNotifications()` now fires for real inside the native build even under `TEST_MODE` (the whole point of this test build), `sendNotif()` branches to `Capacitor.Plugins.LocalNotifications.schedule()` natively instead of the browser Notification API, and a genuinely dormant pre-existing bug was fixed along the way — `requestNotifPermission()` was defined but never once called anywhere in the app, so even a real production web user would never have been prompted for notification permission; it's now wired into startup via `subscribePush()`. Settings' Notifications copy is now conditional on `isNativeApp()`. App icon generated from the real ChemoWell mark at all 5 Android densities (legacy + adaptive foreground/background) plus a proper white-silhouette status-bar notification icon extracted from the mark's cross shape. Build itself runs in GitHub Actions (`.github/workflows/android-build.yml`), not this sandbox — the sandbox's network allowlist blocks Google's Android Maven/Gradle Plugin Portal entirely, confirmed via direct request (403s), so CI is the real build environment for anything native going forward, not a convenience. Debug-signed APK only; a release keystore and store-track billing/SQLite are separate future chain items. QA: full v14 regression suite (8/8) — zero console/page errors across load + 3+ notification-check tick cycles, no horizontal overflow, version string confirmed, Meds/Settings regression-clean. Known gap: on-device notification firing and native look/feel cannot be verified from this sandbox (no Android device/emulator here) — verified via code audit + CI build success only; Owner has the device-testing checklist for the parts that require a real phone. |
| app-v13 | 2026-07-24 | Med-editor merges (Owner-approved R1/R5/R6; LEAN chain run per TEAM.md Owner amendment — Dev brief + Lead Dev + one combined verification pass): the four additive Home-placement toggles are now ONE single-choice "Home screen placement" picker (Own card / Morning / Afternoon / Evening group / No Home card) whose value derives from the same four stored booleans — no schema change, save path untouched; legacy multi-group combos surface as a conditional "Custom (current mix)" option that preserves the exact stored layout so an unrelated edit never rewrites a patient's Home. The two overlapping "as needed" options are unified by dynamic labels: Schedule type reads "As needed — gap timer between doses" / "Scheduled — set time windows"; the Days-taken as-needed option reads "No set days" for gap meds and "As needed — don't flag missed doses" for scheduled meds (its real safety meaning), with a contextual helper line. "Frequency / rule note" → "Notes" with a placeholder that no longer coaches duplicating enforced rules. QA: round-trip invariant verified byte-idempotent by real clicks (canonical, multi-group, win+asneeded), alerts safety derivation confirmed untouched, v12 first-run suite re-run clean. |
| app-v12 | 2026-07-24 | MOBILE-FIRST onboarding fix (Owner-reported) + units-display blocker (caught by the new QA "User Zero" role, added to the chain this release along with the binding mobile-first testing rule — see TEAM.md). (1) Tour occlusion: on every action step (Meds tab, Add, med form, Home) the big guide card — which buried the form and Save on phones — is now a slim banner pinned over the header brand row; "More" expands the full card (gains "Got it" collapse), Skip always visible; discarding the editor mid-step OR navigating away via the bottom nav steps the tour BACK instead of stranding it (audit P3-1); real-tap verified at 390x844 and 360x400 keyboard height (25/25 checks). (2) Gap-hours is a grey placeholder 0 (type directly; empty saves as As-needed) with new helper copy. (3) Redundancy cuts: duplicate editor Cancel, repeated header sub-copy, stale Settings breadcrumb, two dead display:none spans per med card. (4) UNITS BLOCKER (HIGH): switching °F/°C (lbs/kg) relabeled stored readings without converting — 99.1°F showed as "99.1°C" in fever red. Fix: readings stamp their unit at log time (legacy entries inferred from dose string), ALL displays convert to the current unit (Home, journal, history, weight chart/stats/hub), fever/high styling compares the displayed value in the display unit (38.0°C≡100.4°F exact), stored values never rewritten (exact round-trip, no drift); unit-aware temp input bounds; Settings caption updated. (5) Design-system conformance from chain reviews: banner label 12px, all tour buttons + Add pill + editor save 44px, editor title TYPE.title. QA: units suite 14/14, first-run suite 25/25, v11 regression suite re-run clean, QA User Zero full fresh-user walkthrough (91 screenshots) — blocker re-verified RESOLVED clicks-first. Queued (pre-existing): tour replay flow (D1-D4), toast z-index/pointer-events, sheet keyboard a11y, onboarding missed-dose grace, Waiting-after-complete label, check-in first-day feedback, report Back-pill overlap, weight-chart duplicate x-labels. |
| app-v11 | 2026-07-24 | First release through the full Quality Chain (TEAM.md). Fixes both Owner-reported phone bugs: (1) Plans bottom sheet no longer flickers — the 1s tick loop now skips re-render while the sheet is open (upgradeOpen joins timeModal in the tick guard), the sheetUp entry animation plays once per open via a module-level animate-once flag (reset on close, so reopening animates), and the sheet's internal scroll survives interaction re-renders (id-requery scrollTop preservation in render(), clamps when content shrinks after purchase). (2) Pull-to-refresh no longer dumps you to Home — active tab persists in sessionStorage (strict 6-view whitelist, falls back to Home on garbage; reportsView/modals/drafts deliberately NOT persisted; factory reset clears it so a wiped app boots to welcome→Home; profile switch now lands back on Settings), plus html/body overscroll-behavior-y:none suppresses the browser refresh gesture on Android (iOS best-effort — tab restore is the guarantee there). Audit P3-3: eraseAllAppData references UI_VIEW_KEY. Chain artifacts in outputs/: DEV_BRIEF_v11, DESIGNER_REVIEW_v11 (PASS x5), LEAD_DESIGNER_SIGNOFF_v11 (PASS), AUDIT_v11 (0 P0/P1; P2 README row fixed; P3-1/2 pre-existing and P3-4 new-in-v11 cosmetic — all queued). QA: 12/12 fix-specific checks, 60+ audit checks, 15 journeys, zero console errors. Known queued (pre-existing): toast hidden behind sheet scrim (z50<z70), sheet keyboard a11y (focus/Escape), double-tap-scrim insta-dismiss. |
| app-v10 | 2026-07-24 | Design batches 2+3 (per DESIGN_SPEC_B23, 24 items): 7-step TYPE scale (12px text floor, weight 800 reserved for display/CTAs); header rebuilt as a two-row app bar (44px ?/gear buttons, calm amber BETA badge, redundant orange strip deleted, no clock — title + date only per Aaron); bottom nav 56px items with active dot + rose tint, aria-current='false' bug fixed; all nine alert banners unified to one calm notice recipe (white card, 4px tone accent, tinted icon chip, sentence-case ink titles — no red floods); chemo banner reframed supportive ('Treatment day'/'Recovery day'); NEW dose-progress ring in header (44px SVG, taken/scheduled of today's window-med doses, green ✓ when complete, hidden when nothing scheduled — new pure helper doseProgressToday); paywall converted to bottom sheet (drag handle, sheetUp animation + reduced-motion guard, scrim tap-close, Plus featured w/ Most-popular badge + gradient CTA, TEST_MODE handlers untouched); Quick Log dose buttons and ~20 other touch targets raised to 44px; a11y sweep (8 contrast fixes, 12px floor, keyboard access on clickable divs, aria-labels on icon buttons, dialog/toast ARIA, :focus-visible rule, aria-pressed on segmented toggles, meter text alternatives); time-modal dark-theme leftovers fixed; favicon link added (kills 404); empty-journal copy nit fixed. QA: node --check clean, doseProgressToday harness 5/5, Playwright smoke on 360/390/1280px — every tab, fresh-install setup, tour skip, med add, simulated purchase, ring 0/2→1/2→✓ — zero console/page errors, zero horizontal overflow. |
| app-v9 | 2026-07-23 | Design batch 1 (per DESIGN_SPEC): warm ivory canvas with pink band only at top, solid white cards with hairline #EBE3E4 borders (glassmorphism fully removed), deeper AA-compliant rose #A24C71/#8E3D61, darker ink text, gel inset button shadows replaced with soft drops, decorative background blobs deleted. Product fixes: 'On device' sync-era indicator removed; Treatment schedule card now says 'No chemo/radiation date set'; Days taken gains 'As needed (no set days)' — available any day, never flagged missed. Also updated version string to app-v9. |
| app-v8c | 2026-07-23 | Neutral welcome placeholder (no suggested name). Check-in dates rendered as bold highlighted chips so the day jumps out. Bowel Movement gains a "Loose" option (distinct from Diarrhea). New Blood Pressure tracking: Home card (sys/dia, validated, toggleable in Settings) + Blood Pressure report with full history and remove. |
| app-v8 | 2026-07-23 | Audit burn-down (P1/P2): new Settings screen behind header gear — Profiles (now with delete + confirm), plan/upgrades, Home screen customizer (temp/weight/treatment cards toggleable; daily weight / bowel / appetite check-ins now OPT-IN, cycle tracking opt-in), units (°F/°C, lbs/kg), replay guide, About + medical disclaimer, Start over (moved from Meds). Check-ins never escalate red on install day. Zero-med Home shows an Add-first-medication CTA. Tour progress persists across restarts + Back button. Take-all re-validates locks at confirm time; daily-limit unit mismatch blocked in editor. Brandi's regimen data (DEFAULT_MEDS/seedDemo/senokot migration/protonix copy) deleted from bundle. DST-safe day math (hourTs/nextDay). Stale-tab profile-switch guard. Google Fonts removed (system fonts — no IP leak), viewport-fit=cover. 'Chemo schedule' → 'Treatment schedule'. QA: 35/35 harness checks. Hotfix v8b: the seedDemo removal left a dangling `async` keyword that crashed the whole app at load (caught by live smoke test minutes after deploy; node --check passes on a bare identifier so only runtime verification caught it). |
| app-v7 | 2026-07-23 | Audit P0 burn-down: (1) missed-dose tracking now works for user-created scheduled meds (alerts auto-set for window meds); (2) user meds can never take legacy ids (Zofran/Tylenol/etc.) so no one inherits the original patient's lockouts or the silent 2,500 mg APAP ceiling — all CONFIG.ceilingMg fallbacks removed; (3) chemo banner genericized — no more prescribed premeds copy, legacy chips only when those meds exist; (4) Bowel/Appetite yesterday check-ins gated on install date; (5) Erase-all preserves purchases; (6) cloud-era ghost copy purged (check-connection toasts, Connecting…, Brandi placeholder, 156.0, live-sync text); (7) schedule windows auto-sorted (protects missed-dose claiming). QA: 24/24 harness checks. |
| app-v6 | 2026-07-23 | Per-medication day scheduling: new "Days taken" in the med editor — Every day / specific weekdays (7 day-pill toggles) / every N days from a start date. Off-days: quick log + grouped cards show "Not scheduled today," missed-dose logic and grouped Take-all skip them. New "Group with afternoon meds" toggle + Afternoon meds Home card. Schedule badge shown in med manager. QA: 28/28 harness checks. Also fixed a renderer-wide bug found during QA: h() set `value` only as an attribute, so every re-rendered <select> silently displayed its first option instead of the selected one — value is now applied as a property after options mount. |
| app-v5 | 2026-07-23 | Multi-profile + plans: profile-namespaced storage (auto-migrates v1-v4 data into profile p1), Profiles section on Meds tab (list/switch/add), Free (1 profile) / Plus $4.99 (3 profiles + export/backup coming) / Pro $14.99 (unlimited) one-time tiers, plans modal with TEST_MODE simulated purchases + Reset-to-Free for beta testing. Erase-all now wipes every profile + license. QA: 21/21 harness checks (migration, tier gating, switch/reload, downgrade keeps data). |
| app-v4 | 2026-07-23 | "Start over" section at the bottom of the Meds tab: Erase all data… → confirm step → wipes patient name, medication list, and all logged history from the device and reloads to the welcome screen + guide (true factory reset). |
| app-v3 | 2026-07-23 | True from-scratch start: fresh installs get an EMPTY medication list (Brandi's defaults removed; no auto-seeding). New interactive first-run walkthrough: 10-step coach-mark guide that highlights real UI elements and waits for the user's own taps — Meds tab → Add → fill out the form → save first med → back Home → tour of dose logging, Reports, In-Patient, Symptoms. Skippable, never re-nags after skip/finish (prefs.tourDone), relaunchable via new ? button in header. QA: 27/27 harness checks. |
| app-v2 | 2026-07-23 | Fresh-install fix: missed-dose tracking now starts at `installedAt` (stamped into prefs on first run) instead of a hardcoded past date — a new install no longer shows dozens of phantom "missed doses from previous days". |
| app-v1 | 2026-07-23 | Seeded from chemowell-beta v71. Firebase/Firestore/FCM fully removed → on-device storage layer (entries + prefs in localStorage, same function names: subscribeEntries/addEntryDB/removeEntryDB/subscribePrefs/setPrefsDB). First-run welcome screen asks patient name (stored in prefs; no hardcoded Brandi). "Live sync" indicator → "On device". APP BETA badge/banner. SW cache chemowell-app-v1. Storage layer QA: 10/10 Node harness checks. |
