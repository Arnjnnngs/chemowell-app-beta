# ChemoWell (APP-BETA)

Track medications, doses and symptoms through cancer treatment — for patients and caregivers. **All data stays on the device — no cloud, no accounts, no tracking.**

This is the native-app codebase (APP-BETA in the 4-target system). It was seeded from `chemowell-beta` (WEB-BETA) at v71 and differs in one fundamental way: Firebase/Firestore is fully removed and replaced with an on-device storage layer (`localStorage`), plus a first-run setup flow. It will be wrapped with Capacitor for Play Store / App Store distribution.

- Live test build: https://arnjnnngs.github.io/chemowell-app-beta/
- Sibling repos: `care-tracker` (WEB-MAIN, the owner's live app) · `chemowell-beta` (WEB-BETA, staging for WEB-MAIN)

## Version history

| Version | Date | Changes |
|---|---|---|
| app-v70 | Aug 31, 2026 | **Remove ONE treatment date without wiping the schedule — plus the Help corrections the audit of the unreleased v69 work demanded.** **(1) The feature.** A course has several treatments and the app already obeys every date it holds, but the Treatment schedule card only ever showed the *next* one and **Clear** was all-or-nothing — so a single mistyped date could sit there driving medication rules with nothing on screen admitting it existed, and correcting it meant wiping the lot and retyping. The card now lists every date it is holding (whenever there are two or more; a single date is already named in the headline above it), each with its own 44px **Remove**. **How the removal is stored, and why it matters.** The store is append-only and a backup taken today can be restored into an *older* build, which knows exactly two `chemo_date` shapes: `ts > 0` = a date to add, `ts === 0` = clear everything. So removal invents **no new shape** — it appends a tombstone and then re-adds the dates being kept. A negative-`ts` "remove this one" row was considered and rejected: an older build would read it as a date to ADD, and `dayStart(-X)` lands in 1970 — the exact *"Zofran blocked until 1 Jan 1970"* defect this project already shipped once. `chemoDayList()` is untouched. The tombstone's `loggedAt` is `Math.max(Date.now(), latestLoggedAt + 1)`, because two removals in the same millisecond could otherwise order the tombstone *after* its own re-adds and wipe the schedule on the next launch. **(2) Help said the wrong thing, three more times.** The Zero Day Auditor's verdict on the v69 work was **DO NOT SHIP**, and it was right. The corrected treatment-date topic told the reader to tap **Update** — but its own step 3 sends them through **Clear** first, after which that button reads **Set date**. It also described confirming a clear by *"tapping it again"*, when the confirm is a separate **Confirm clear** button that gives up after 6 seconds. Both fixed, and the topic now documents per-date removal. Separately, the app still claimed in **five** places that **medication logging pauses during a hospital stay** — something that stopped being true in **app-v67**. An earlier version of this row said three, because the fix was driven by grepping for one phrasing, *"logging pauses"*; the survivors said *"logging **is** paused"* and *"goes back to normal"*, and **a grep for the sentence you have already fixed always comes back clean.** The Zero Day Auditor found the two that were missed and the worse of them was not in Help at all — it sat on the **In-Patient screen itself**, directly under the amber Active chip, contradicting the Home banner, the FAQ, the welcome tour and its own help icon in the same build. All five now say what actually happens: everything stays loggable, and dose windows that open during a stay are counted as the hospital's rather than as misses. A sixth string was tightened while the file was open — Help said *"days inside a hospital stay are never flagged as missed"*, which is the old whole-day model; the real rule is per dose window, judged when the window opened, so a stay ending at noon leaves that afternoon and evening tracked normally. **(3) The gates.** `test/v70-remove-one-date.mjs` **12/12** pins the storage shapes; `test/v70-remove-one-date-browser.mjs` **15/15** clicks the real button in a real browser, because the unit suite cannot see a button that is never rendered or never wired — the same blind spot that let a temporal-dead-zone bug wipe every saved medication while four unit suites stayed green. `test/v69-treatment-date-help.mjs` grew from 9 to **19/19**: its warning check used an alternation that matched a *different* sentence and stayed green when the auditor deleted the warning outright, and **no check compared Help's button names against the code at all** — this took **three** attempts and the first two are worth recording. A fixed list of names checked each one only *if* Help mentioned it, so renaming a button in Help made its check stop running and the suite reported 17 of 17. Driving it off Help's own `tap **X**` fixed that and opened two more, both proven by the auditor: writing *press* instead of *tap* skipped the extractor entirely (18/18, exit 0, Help pointing at a button that exists nowhere), and naming **Paracentesis** printed *in green* "the card really renders a button called that" — because `card` was a blind 6000-character slice that ran past the end of this card and would equally have matched `'flex'`, `'center'` or a toast string. **A check that prints a false sentence in green is worse than no check**, because the total still reads healthy. It now bounds the card by matching its own parentheses, parses the real button labels out of the `h('button', …)` calls inside it, checks the extractor itself with a name that must be present and one that must not, accepts any instruction verb, and requires every capitalised bold name in the steps to be a real button — so an invented control cannot hide behind a missing verb either. Both auditor escapes now go red, as do a no-verb capitalised invention and a rename of the real button on the card. A residual gap the auditor found afterwards is closed too: the check read only the topic's **steps**, so a control invented in the **answer paragraph** — which the reader sees first — passed at 26/26. It reads both now. **`test/v70-stay-does-not-lock.mjs`** is new and exists because none of the above was guarded by anything: no suite in `test/` mentioned those strings at all. **It is an allow-list, and the two versions before it were blacklists that did not work.** The first enumerated phrasings that mean *logging is locked*, with a narrow exemption for prose that legitimately describes the OLD behaviour. The auditor got **four** false claims past it on the In-Patient screen in one sitting: *"logging is **suspended**"*, *"you **can't log** medications"* (the rule required the passive *cannot be logged*), *"every **dose button is locked**"* — which never says the word *logging*, and three of the seven rules required it adjacently — and the **verbatim pre-fix sentence** dashed onto a clause containing *was*, which the exemption then released, because the sentence splitter bounded on `. ? ! "` and this codebase writes in em-dashes on nearly every line. The exemption was holed three separate ways in three attempts (a ±220-character window, a preceding question leaking its tense forward, then the intra-sentence dash), which is the signal that the **shape** was wrong rather than the radius. And the deeper fault was the same mistake this release was opened to fix, rebuilt one level up: a family of phrasings **of the sentences already fixed** is not a family of **meanings**. English has unlimited ways to write *you cannot log*; this file has a countable number of sentences that actually mention a stay. So the list is inverted. Every live sentence that mentions a stay **and** mentions logging, doses, medications, cards, buttons or missed-dose tracking must appear in the suite verbatim — 33 of them today. Rewording one, or adding one, fails until a person reads it and puts it there on purpose. The failure is *"nobody has checked this sentence"*, not *"this sentence matched a bad pattern"*. **The ground-truth check was worse than useless and is rewritten too.** It counted occurrences of one helper's name. The auditor put a real lock at the top of the dose path using a *different* helper — genuinely blocking every dose log during a stay, the exact app-v67 regression, with the prose now the thing that was lying — and the suite printed **all checks passed**, including the green sentence *"a stay still touches exactly one rule"*. It now reads the two functions that actually write a dose and requires that neither mentions any in-patient predicate, whatever it is called. Falsified **eight** ways: the four wordings above, the verbatim pre-fix sentence, the real lock on the dose path, a rename of every stay word so the scanner goes blind, and the scanner losing sight of the prose entirely. All eight red, each for its own reason. `test/overflow-scan.mjs` is **170/170 clean** across 17 passes × 10 widths. **(4) A fixture that swapped coverage instead of adding it.** Seeding an open hospital stay and a second treatment date fixed two real blind spots and opened two more in the same move: the In-Patient screen with **no** stay, its empty state, Home without the banner, and the Treatment schedule card with no date at all stopped being scanned anywhere. Three new passes end the stay and clear the dates — last, deliberately, since they change state and do not put it back. **One of the three had a receipt that could not fire:** `no-stay:home` guarded with a regex for *"Day N of ... stay"*, wording the banner has never used (it says *"In-Patient active"*). The auditor forced the banner to render permanently and got a byte-identical 170/170 CLEAN. The needle is now the string actually on screen, and forcing the banner now makes the pass report unreachable. **(5) `test/v55-help.mjs` has been red since app-v67 and nobody ran it.** It searched for a Help topic by a title that v67 deliberately rewrote. Pointed at the same topic by its current name; ALL GREEN. **(6) A backup restored from an older lineage could keep a date it was told to delete.** `chemoDayList()` sorts on `loggedAt || ts`, while the removal stamped its tombstone from `loggedAt` alone — so a `chemo_date` row carrying no `loggedAt` would sort by its `ts`, a future date larger than `Date.now()`, and survive its own removal. Nothing this codebase has ever written omits `loggedAt`, so it was only reachable through a restore; raised by the auditor as a watch item and closed here instead, one line, with a check that goes red on the old code. **Versioning:** `APP_VERSION` → `app-v70`, `sw.js` CACHE → `chemowell-app-v70-1`. **app-v69 was never published** — the Help and layout work carrying that name was committed without a version bump (the audit's headline blocker: every installed phone would have kept serving the old cached build, silently, forever) and ships here instead. |
| app-v68 | Aug 29, 2026 | **HOTFIX — clearing a treatment date did not clear it, and two leftovers from app-v67.** Found by the Zero Day Auditor on the retroactive audit of app-v67, which **FAILED**. **(1) BLOCKER:** clearing a treatment date is append-only — the Firestore rules forbid edits, so **Clear** appends a *tombstone*, a `chemo_date` row with `ts: 0`. app-v67's new `chemoDayList()` filtered `ts > 0`, which discarded the tombstone and **kept the date it was meant to erase**. `nextChemoTs()` honoured the clear and `chemoOffsetFor()` did not, so the app held two contradictory beliefs: **Zofran stayed blocked showing a 1 Jan 1970 unlock time**, Dexamethasone kept raising missed-dose alerts against a deleted schedule, and a mistyped date could never be taken back. It now walks entries in `loggedAt` order and a tombstone wipes every date recorded before it; a new date set afterwards starts fresh. **(2) Zofran's block is directional.** It runs treatment day through +2, and *nearest date* answers a different question — with treatments on 24 and 27 Aug the 26th is one day **before** the 27th and two days **after** the 24th, so nearest returned −1 and silently unblocked a day that should be blocked. `chemoOffsetSinceLast()` answers that separately; Dexamethasone's −1..+1 window is symmetric so nearest stays right for it. **(3) Two leftovers from deleting the Restricted tiles**, both of which the two web builds removed correctly and this one did not: `!inpatientNow &&` forced `offDay` false during a stay, so an unscheduled medication showed a full **Log** button that did nothing when tapped and an override prompt reading *"Closed — opens later"* instead of *"Not scheduled today"*; and **Take all** was hidden for the whole stay. The rewritten Help topic promises *nothing is locked* — these made that untrue. `APP_VERSION` → `app-v68`, `sw.js` CACHE → `chemowell-app-v68-1`. **Two more MAJORs from the audit, neither introduced here, both of which I had already written up as fixed.** **(4) The directional fix reached one caller out of two.** `treatmentActiveOn` — the editable per-medication version of the same rule — still asked for the *nearest* treatment. Treatments on 24 and 27 Aug with a 0-before/3-after window: the 26th is two days after the 24th and inside the window, but the nearest date is the 27th, so it measured −1 and the medication vanished. Both directions hurt and one is worse — a treatment-only medication disappears and its misses go unreported, while an **excluded** medication is *offered on a day it should be withheld*. Both `treatmentActiveOn` and `zofranBlockedOn` now ask **every** treatment date. **(5) The wrong date was still being printed.** Display sites read `dayStart(nextChemoTs())` — the most recently *entered* date — while the condition around them used the offsets, so a card correctly refused to unlock and then read *"Opens Thu, Aug 6"*, three weeks past; with no date at all it printed **1 Jan 1970**, the same label these notes previously called resolved. `chemoDayFor()` and `zofranBlockingDay()` return the date the decision was actually made from. **(6) The treatment window was unbounded here** — a `text` input with no upper limit, so a mistyped 30 would have made a medication treatment-adjacent for a month either side. **Correction, from the Zero Day Auditor:** an earlier version of this row also said every window typed in "silently collapsed back to 1/1" because `Number.isFinite("3")` is false. **That never happened in a shipped build.** `saveMedicationEditor` has clamped that field to a real number since app-v16, the release that introduced the feature (`git log -S` confirms it), so nothing the medication editor saves has ever been a string. The `normalizeMedication` fix is still right — that is the one place every saved medication passes through, and a restored backup or an imported file can hand it a string — but it closed a hole rather than fixing something users were hitting. The badge-versus-behaviour divergence ("−300/+300" on screen while the app obeyed 14) was likewise created and removed inside this same release; before it there was no upper bound anywhere at all. **Correction to an earlier claim in this row.** It said the suite *"goes red on app-v67"* with 5 failures. **It does not — it cannot start there**, because `chemoOffsetSinceLast` does not exist in app-v67, so it throws before a single assertion runs. That is a could-not-start reported as a red, which is the exact confusion this project has been fixing elsewhere all week. The substance held up: the auditor falsified it properly with mutants of the v68 file — old `chemoDayList` → 13/17, old `zofranBlockedOn` → 16/17, both → 12/17. Gates: `test/v67-chemo-offset.mjs` **26/26**; `test/v67-inpatient-window.mjs` 10/10; `test/v57-search.mjs` ALL GREEN. |
| app-v67 | Aug 26, 2026 | **A hospital stay no longer locks you out of logging, and treatment-day maths no longer reads the wrong date.** Ported from care-tracker v60 after Aaron hit all of it on a real half-day admission. **(1) Medications were unloggable for the whole of a stay.** Every card became a dead *In-Patient (Restricted)* tile, and this app went further than its twin: the code said in-patient *"wins outright (hospital-administered, not overridable)"*, making it the **one** blocked state with no *log it anyway* escape — while Waiting, Limit, Not-scheduled and Outside-treatment-window all had one. Worse, the Help centre **taught the workaround**: *"If the stay is over, tap Log In-Patient End. Medication logging goes back to normal straight away."* So the app instructed you to end a stay early — falsifying when she came home — in order to record a dose she really took. Aaron: *"in the evening I had to end in patient bc I couldn't enter the Dex for the evening."* In-patient now falls through to the same override-capable row as every other reason, and **both Help topics are rewritten** (one now carries the note that it used to say the opposite). **(2) Missed-dose suppression was all-or-nothing for a whole calendar day.** A stay starts and ends at a *time*. Suppression is now decided **per dose window** — the hospital's if she was admitted at the moment that window opened. Home by 5:51 PM with an 8 PM window? That one is yours, tracked normally. **(3) The wrong treatment date was used for every day in history.** `chemoOffsetFor()` measured from the most recently *entered* date rather than the one nearest the day being asked about, so logging a past treatment after a future one shifted the whole timeline — every *Dexamethasone expected?*, every *Zofran blocked?*, every window count. It now takes the **nearest** date, and duplicate dates on one calendar day collapse to one treatment. **(4) `backfillDefaultMedFlags()` carried across** — inert here, and deliberately so: this app ships `DEFAULT_MEDS` **empty**, so the saved-config drift that caused three weeks of false alerts in care-tracker cannot occur. `test/v67-medflag-backfill.mjs` pins that reasoning and goes red the day a default medication is added. `APP_VERSION` → `app-v67`, `sw.js` CACHE → `chemowell-app-v67-1`. Gates: `test/v67-inpatient-window.mjs` 10/10 **falsified** against the pre-fix code, `test/v67-chemo-offset.mjs` 9/9 (runs on care-tracker's real treatment dates and asserts the exact banner line Aaron screenshotted is gone), `test/v67-medflag-backfill.mjs` 4/4, `test/v57-search.mjs` ALL GREEN. |
| app-v66 | Aug 24, 2026 | **The app change is one hidden search word and two version numbers. Everything else here is the tests, and that is the real story.** **In the app:** `"litres"` is restored to the paracentesis Help topic's *search keywords* — a list nobody ever sees, which exists only to catch what someone **types**. app-v65 deleted it along with the visible British spellings. **An earlier draft of this note claimed that broke search. It did not, and the claim is corrected here rather than quietly dropped:** the topic was always found, and always found first. What changed was match *quality* — a 0.65 fuzzy guess that also dragged an unrelated topic into the results, versus a clean exact hit returning the one right topic. Small, real, and not worth a release on its own. **What the release is actually for:** `V57-1`, the check guarding the *"contact the care team"* disclaimer on the Help search screen, **had been failing since app-v58 and nobody noticed** — it pinned the sentence word-for-word and app-v58 reworded it. Eight releases shipped past a red safety check. **The app was never unsafe:** the disclaimer and the one-tap route to the emergency page are both present and always were. The check now asserts the two things that matter — the screen disclaims medical authority, and it names the care team — and lets the wording change. **The spelling gate took three attempts, and the first two were mine and wrong.** Attempt one searched the raw file text for the keyword list. The Zero Day Auditor beat it in minutes: write the characters `keywords: [` into ordinary Help wording and the gate treats everything after it as safely invisible, so a build with the alias **deleted** *and* a visible **"Litres"** in a Help answer passed **16/16 all green**. It also hardcoded "exactly one", so adding the alias to a second topic would have gone red for nothing — the same hardcoding mistake this release exists to fix, committed inside the fix. Attempt two read the *parsed* Help data instead, which no wording can fool — but narrowed the net without anyone noticing: most of the ten strings app-v65 fixed live **outside** Help (the input box, the error toast, the Reports empty state), and a test build with a visible "Litres" in the input box sailed through. Attempt three adds a count cross-check, which prose cannot hide from. **A second audit then cleared it**, including against an attack nobody had built — a visible "Litres" in a Help *category* description — and found three more of my errors: a retracted false claim still stated as fact in a second file, a check that counted source **comments** (a helper built for exactly that problem sits in the same file, its own notes recording three past incidents), and a wrong description of how the original break behaved. All corrected. **Also fixed:** a literal `|| true` at `outputs/webmain-v43/run.mjs:119` that made an assertion permanently green; **19 browser suites plus one archived runner** in this repo hardcoded a playwright path from the retired sandbox and could not start at all in a cloud session (a gate that cannot start is indistinguishable from one that passes); and `release_check.sh` / `mark_published.sh` were committed `100644`, so `./release_check.sh` would exit 126 for anyone who cloned the repo — both now `100755`. **Found but NOT fixed here, logged in `BACKLOG.md`:** app-v58 also undid two of the v57 Designer's fixes to this same screen — the care-team strip lost its own background colour and the results count moved back below all twelve rows, leaving the first real answer **21px visible** above the bottom nav at 320px. Red for nine releases. And **nothing runs these suites** — `npm test` is a stub that exits 1, `release_check.sh` runs none, CI runs none — which is the mechanism behind both blind spots. `APP_VERSION` → `app-v66`, `sw.js` CACHE → `chemowell-app-v66-1`. Tests: `test/v57-search.mjs` ALL GREEN (53/53, **6 new checks**, each falsified against a deliberately broken build), `test/v59-para.mjs` 15/15, `test/v64-logger.mjs` 23/23, `test/v63-encrypted-backup.mjs` 22/22, `release_check.sh` exit 0 (with its two `PUBLISHED.json` warnings disclosed, not summarised away). |
| app-v65 | Aug 24, 2026 | **One spelling for "liter", everywhere.** Aaron: *"for the para.. is it supposed to be 'Litres' or Liters?"* It was both. Every identifier used the American spelling (`PARA_MAX_LITERS`, `paraFmtLiters`, `e.liters`) while **ten strings used the British one** — the Home card's input placeholder, the validation message, the Reports empty state, the Settings toggle description, and a **Help page**, which is the one a confused user actually reads. Normalised to **liters**; no identifier touched. **The gate asserts it by absence against the shipped bytes**, not against a screen: a screen check only covers the screens a suite happens to visit, and two of the ten were in copy no suite opens. `test/v59-para.mjs` 16/16, **falsified against app-v64 where it reports all ten**. **Also fixed: this app's logger suite pinned a version literal.** `test/v64-logger.mjs` asserted `'app-v64'` and broke on the very next release — the same anti-pattern that had already cost this project three patches, and the same one its CareTracker twin was fixed for a day earlier. It now reads `APP_VERSION` from the file under test. **Recorded, not changed:** liters is the standard unit for paracentesis in every country — the UK spells it differently, it does not measure it differently — so no volume-unit picker was added. This app already offers °F/°C and lbs/kg with per-entry unit tagging. `APP_VERSION` → `app-v65`, `sw.js` CACHE → `chemowell-app-v65-1`. |
| app-v64 | 2026-08-22 | **The app now writes down its own errors, and gives the person a place to add theirs.** Aaron: _"we were also going to build in a logger for errors or improvements. so many thing I've said has gotten lost."_ ChemoWell has no backend and no crash reporting, so until now a beta tester who hit a real error had exactly one route to telling anyone: remember it, and describe it later from memory. **In the app:** a new drawer row, **Report a problem**, directly under Help & FAQ — because the two are the same journey, and a tester who cannot find this row is a tester whose bug report never gets written. The screen does three things in the order a person needs them: say what happened (*Something went wrong* / *An idea to improve it* — Aaron asked for **both**, not just crashes), see what the app already noticed on its own, and take the whole lot away as one plain-text file. **The app's own half is automatic.** A passive `error` listener and an `unhandledrejection` listener record faults as they happen — the second matters more than the first, because most real failures in this app are inside an `await`, where `window.onerror` never fires. Neither handler `preventDefault()`s: the gate asserts a thrown error is **still thrown**, since a logger that eats the error hides it from the console and from every other tool. **The logger must never become the fault**, and three checks hold it to that: one error repeating on every render tick is **one entry with a count**, not sixty (`x40` in the gate); the list is capped at 100; and a `QuotaExceededError` on a full phone is swallowed by the logger rather than turned into a broken screen — the write happens on the error path, which is exactly when storage is most likely to be gone. **One real defect was found by the gate and fixed before this shipped:** a straight ring buffer let a flood of errors **evict the person's own description of the fault they were reporting** — the half nobody can reconstruct afterwards. Trimming now drops the oldest *errors* first and takes what the person wrote last; `LOG-11b` pins it at 180 errors and 0 lost reports. **The file gives nothing away.** It carries the app version, the device string, the screen size, the errors and what the person typed — and **not one medication, entry, appointment or note**. The gate seeds a medication and a note with distinctive names and searches the produced file for both. The file says so in its own header too, so the sender can check the promise by opening it rather than taking it on trust. Nothing is ever transmitted on its own: there is no *send* button that silently posts anywhere, because there is nowhere to post to and a button that pretends otherwise is worse than none. Clearing the list takes two taps. `APP_VERSION` → `app-v64`, `sw.js` CACHE → `chemowell-app-v64-1`. Test: `test/v64-logger.mjs` — 23 checks, **falsified against app-v63 where 20 go red**. |
| app-v63 | 2026-08-22 | **Backup files can now be protected with a password — asked for twice, built here.** Aaron: _"build the encryption part."_ A ChemoWell backup is the whole medical record for a profile in one file, and the entire point of the feature is that people **send** it — to a second caregiver, by email, through a shared folder. Every one of those routes leaves a copy somewhere neither person controls, and until now every copy was plain text. **In the app:** a switch above the two backup buttons, *Protect the file with a password*, off by default. Turn it on, type the password twice, and the saved file is **AES-256-GCM** under a key derived from that password with **PBKDF2-SHA256 at 310,000 rounds** — the same three `crypto.subtle` primitives already in this file for sync, so no new dependency and no server. Opening one asks for the password first and shows **no manifest at all** until it opens, because naming the patient and the record count on the locked screen would leak exactly what the password is protecting. **The file gives nothing away:** the profile name lives inside the ciphertext, and a protected backup is named `chemowell-backup-protected-<date>.json` rather than carrying the patient's name in the filename of a file meant to be emailed. Only `format`, `formatVersion` and `createdAt` sit in the clear. **It fails closed, and the gate proves each way it can:** a wrong password opens nothing and writes nothing (storage checked byte-identical after); **one flipped byte of ciphertext is rejected** rather than half-restored, because GCM authenticates; an **iteration count read out of a file is bounded, not trusted** — a hostile `900000000` is refused in 0 ms instead of freezing the phone for minutes; and a file that decrypts perfectly but is not a backup is still refused, because decrypting proves *who wrote it*, not *what it is*. **Version numbering is split so an old phone says the right thing:** plain files are still written at `formatVersion: 1` so app-v62 and earlier can read them, while protected files are written at **2**, which makes an older build report *"made by a newer version — update first"* instead of opening an envelope it cannot read and calling the backup empty. **There is deliberately no recovery path.** The password is never stored, never transmitted, and not derivable from the file — the gate sweeps every `localStorage` key to prove it — and it is cleared from memory once the file exists. That is the property that makes the file safe to send, and it is why the UI asks for it twice and says, in the panel, to write it down and to send it by a different route than the backup itself. `APP_VERSION` → `app-v63`, `sw.js` CACHE → `chemowell-app-v63-1`. Test: `test/v63-encrypted-backup.mjs` — 22 checks, **falsified against app-v62 where 16 of them go red**. |
| app-v57 | 2026-08-13 | **The help bubble is removed, the browser build tells testers what it can't do — and the search box stopped answering emergencies with app pages.** Aaron, after using v56: _"I don't really care for the 'bot'. it's not doing what I want. but I do think all of those things can be in a FAQ under the 3 hamburger menu."_ **The bubble is gone entirely** — the floating button, the panel, the transcript, the composer, and the medical/person guards that existed only because a chat box answers in prose. ~46,000 characters deleted. `test/v57-search.mjs` asserts *by absence* that no bubble symbol survives anywhere in shipped code, so it cannot creep back one helper at a time. The menu row, and the eyebrow on all four Help screens, now read **Help & FAQ**. **The scored matcher stays** — it is what the Help centre's search box runs on, and reverting to v55's all-words-must-appear filter would be a straight downgrade. **Then the Zero Day Auditor disproved the reason given for keeping it that way, and he was right.** The release notes claimed removing the guards was safe because the search box "only ranks a list of page titles, exactly as v55 did and every v55 gate passed". He did not take that on trust: he sliced v55's *actual* search out of commit `864aeaf` and ran the same 50 clinical questions through both. **v55 returned nothing for 49 of 50** — the honest empty state. **v56/v57 return a ranked list for 45 of 50.** That behaviour arrived with the v56 matcher, on a path nobody had audited, because v56's review was aimed at the bubble. In the running app at phone size: _"she collapsed"_ ranked **"All my medications vanished from Home"** (a true keyword hit on `collapsed`, which is why no threshold anywhere catches it); _"she is unresponsive"_ ranked **"I tap something and nothing happens"**; _"he has chest pain"_ ranked **"Can I record how bad it was?"**; _"she is coughing up blood"_ returned **66 rows** headed by "How do I record blood pressure?". Nothing on that screen said the app holds no medical information. **The fix does not restore the guards** — Aaron cut the bubble because a thing that answers questions was not what he wanted, and the guard was a classifier that took two Highs and two PM NO-GOs trying to decide which questions were frightening. So it stopped trying to decide. **The care-team sentence and a one-tap route to the emergency page now sit above every search-results screen, unconditionally** — the Auditor suggested showing it "when the query resolves poorly"; unconditional is stronger and removes the classifier entirely, and one calm sentence costs a person searching "export csv" nothing. A **relevance floor** cuts the tail carried by one weak term (66 rows → 19) and a **cap of 12** holds the list to a phone, with the true total still reported as *"The closest 12 of 19 matches"*. **A new help page, "Side effects — hair loss, sickness, tiredness: is this normal?"**, says plainly that ChemoWell holds no information about any drug, has no idea what is normal for this person's treatment, would be guessing if it said otherwise, and that this is a question for the care team or the pharmacist. **No new medical claim is made anywhere in it.** It was added because the Lead Developer's own suite failed twice on its first run: _"will she lose her hair"_ ranked **"What happens if I lose my phone?"** first on the word *lose*, and only 19 of 50 clinical questions surfaced a care-team page. **The browser build now introduces itself.** Aaron is sharing the beta as a plain URL months before either store listing exists, and a browser tab is not the phone app. A one-time card on Home — browser only, never in the native build, waiting for the first-run guide to finish — says three things: **add this to your Home Screen** (iOS Safari erases a site's saved data after 7 days without a visit, and nothing logged here is stored anywhere else, so this one can cost a tester everything they entered); **reminders only arrive while the page is open**, because a browser cannot wake a locked phone; and **exports download instead of opening the share sheet**. The whole body branches on iOS vs Android and on already-installed, not just the first sentence — the first cut showed Android testers a paragraph about Safari and called it "the one that matters". **The Designer failed it too, on three Must-fixes, and every one was real.** The card measured **919px tall at 360px and 1,121px at 320px** — taller than the phone — with body copy running in a **207px column** because the icon and the ✕ were flex siblings of the entire body: **24 characters per line** against a comfortable floor of 45, and the card was **37% of Home's total scroll length**. It is now **548px at 360 with a 291px column**, and the detail that came out lives in the `rem-web-vs-app` walkthrough the card deep-links to. The **✕ was 28×28px** — 40% under this project's floor — and, because of the card's height, the only control on screen when it first painted. And reverting the toast offset that v56 had raised to clear the bubble put the toast **34px into the "Back to reports" pill**, same dark fill, for its full 4.5s life. **Also fixed:** dismissing the card stranded the viewport mid-page; the read-more button dismissed the notice *and* landed on a page answering none of it, so the one person who tapped it to read more was the only person who could never get it back; the `HELP & FAQ` eyebrow this release rewrote measured **2.79:1** and failed AA; and the new care-team page wore the same **red call-now** treatment as *"something new and frightening is happening"* — the exact inversion the v55 Auditor comment above that line was written to remove, now split by a `careTone` flag. **Two tests had stopped guarding, in opposite directions.** `EXPECTED_TOPICS` was pinned at 117, so adding one help page broke **six** assertions that were checking nothing wrong — it now reads the count from the source and asserts the invariant (the number the landing copy shows must equal the number that exists). But deriving it made **deletion** invisible, so the floor is ratcheted at 118 and a new check catches a removed topic regardless of the count: no topic's `related` list may point at an id that does not exist. The care-team coverage floor read the top **8** while only **4 rows fit above the fold**, and was set-based — so dropping `careLead` from **`sym-severe`**, the emergency page the metric exists for, still printed ALL GREEN. Both fixed. **Manifest wording fixed** — `manifest.webmanifest` and `package.json` said *"for chemo patients"*, which is what a tester reads under the icon on Add to Home Screen and has been wrong since v33. **Removed with the bubble:** `test/v56-matcher.mjs`, `test/v56-helpbot.mjs`, `test/v56-guard-heldout.mjs`, `test/v56-shots.mjs`, and the unused `send` icon. The v56 row below is left as written — it is the record of what v56 shipped. `APP_VERSION` → `app-v57`, `sw.js` CACHE → `chemowell-app-v57-1`. Tests: `test/v57-search.mjs`, `test/v57-browser-notice.mjs`, plus v55 (146) and v52 (20). Reports: `outputs/DEV_v57.md`, `outputs/DESIGN_v57.md`, `outputs/AUDIT_v57.md`. |
| app-v56 | 2026-08-11 | **The on-screen help bubble — tap it, type a question, get the answer back.** Aaron, after seeing what v55 shipped: _"I wanted the bot to be like and on screen help like most apps have them down in the bottom corner. they can click on it and start typing and the bot will look up their question and reply. like AI, but its not AI, its just things that has already been written to the app."_ **v55 built the right content in the wrong shape.** It put 117 walkthroughs behind a menu row as a browse-and-search screen; he meant the thing every consumer app has in the bottom corner. That is a different product, not a polish pass, and it is worth recording as a miss rather than as an enhancement. **What a user sees:** a button in the bottom-right of every screen — not during setup, the guided tour, the menu, any pop-up, or on the Help screen itself. Tap it and a panel opens over the page, **capped at half the screen height** so the app stays visible behind it (Aaron: _"scrolling is fine. doesn't need to cover more than half"_ — long answers scroll inside the panel rather than the panel growing). Type in plain words. It replies with the matching help page: the answer, the first three steps, and a button through to the full walkthrough. If the question is ambiguous it offers up to four pages to choose from; if it doesn't know, it says so and offers the full list. One **X** closes it back to the bubble, and the conversation is still there if you reopen it. There is no separate minimise button, by Aaron's decision. **It is not AI and never pretends to be.** No model, no API key, no network — it works in aeroplane mode, because it is a lookup over the 132 help documents already inside `index.html`. No typing indicator, no delay, no avatar, no name: the reply appears in the same frame as the question, because anything that looks like thinking is claiming to be something it is not. It never reads the patient's data. What you type is never saved anywhere and is gone when the app closes. **It will not answer a medical question.** Anything about a dose, whether something is safe, side effects, interactions, or how someone is feeling is routed to the care team instead — and where the app has a care-team page for it, that page's own words are shown. **No new medical wording was written for this release**; every medical sentence it can say already existed and already went through the v55 review. **Under the hood.** The old search was an all-words-must-appear filter: right for a search box, useless for a sentence, because *"why cant i type in the daily limit box"* returned nothing at all. It is now one scored lookup shared by the bubble and the Help screen's search box — a 139-word stopword list, a short stemmer, a 19-entry synonym map (`meds`→`medication`), typo tolerance, seven weighted fields, and five gates that decide between answering, offering choices, and admitting it doesn't know. Measured on 53 questions a caregiver would actually type, typos included: **53/53 put an acceptable page first, ~1.4 ms per query**, and all 117 walkthroughs are still found first by their own title. **Five review stages ran, and every one of them found something.** Designer (`outputs/DESIGN_v56.md`, 74 screenshots at 320–768 px): 3 must-fix, including a send button that was using the app's own **back** arrow. Auditor (`outputs/AUDIT_v56.md`, 452 cases, verdict NOT READY): 2 High — *"is this medicine safe for children"* answered the **privacy** page, and the panel stayed open over whatever screen you navigated to, with that screen's buttons unreachable behind it. Lead Designer (`outputs/LEAD_DESIGNER_v56.md`) and Lead Auditor (`outputs/LEAD_AUDITOR_v56.md`) then found that **three of the fixes were themselves wrong**: the medical guard had over-corrected and was refusing 21 ordinary questions including *"is my data safe"*; the help bubble had been made white and measured **1.00:1** against the white cards behind it on a screen with real medications in it (the empty test profile never showed it); and two new tests passed whether the code worked or not. All fixed. The medical guard was rebuilt as unconditional rules plus softer word-matches that only apply when the question is not obviously about the app, and it is now measured **in both directions**: **0 of 35 ordinary questions refused** (was 21) and **39 of 50 medical questions caught** (was 10), with the remaining 11 falling through to "I don't have an answer" — which already says it is a question for the care team. **Then the PM gate returned NO-GO, and it was right.** The suite that proved the safety property was green — but every question in it had been used to tune the patterns, so it was measuring the fit, not the property. The PM took questions nobody had tuned on and broke it immediately: *"can I stop the chemo early"* got a **confident answer with steps** about reminder timing, because *early* matched that page and *chemo* contributed nothing. A held-out fixture of medical and ordinary questions **written by the gate and never used to tune anything** — 86 medical and 66 ordinary by the time it was done — is now committed as `test/v56-guard-heldout.mjs`, and the numbers it reports are printed separately so they can never be quoted as the tuned ones. The gate then broke it a second time, on the fix: adding the app's own medication words (`tablet`, `dose`, `pill`) to the list of things that mark a question as being about the app gave a free pass to the commonest medical phrasing there is — ***"is the tablet safe"*** answered the privacy page, and the first word under the question was **"No."** 14 of 14 such questions were wrong, and **neither fixture could see it**, because both were written before those words existed. Those words now mark a question as app-related for one rule and not for the other, and the gate's 14 questions joined the held-out file. On the held-out set as it now stands: **9 of 86 medical questions get a list of app pages, none gets a confident answer**, and **3 of 66 ordinary questions are wrongly routed to the care team**. Both counts are ratcheted in the test, so they can only get worse if someone edits the file and says why. What fixed the confident-answer class was not more medical words but two structural floors: a single content word can never produce a confident answer (*"she collapsed"* used to confidently answer *"All my medications vanished from Home"*), and when the sentence is about a person rather than the app, the winner has to be much further clear of the runner-up. **Also fixed:** typing "help" into the Help screen's search box returned *"Nothing matched that"* — the box had inherited the bot's stopword list, where `help` exists so a chat box doesn't answer a bare "help" with the emergency-symptom page; the two lists now differ on purpose. `HELP_POINTERS` joined the search index (a `BACKLOG` item, now deleted). The "are you a real person" reply was a dead end with no route out. A toast was painting over the panel. And `release_check.sh` now fails the release if this README row does not name the cache key actually shipping — the same row was wrong about it on app-v55 and again here, and a version-history entry wrong about the one fact it exists to record is worse than none, because it will be believed. `APP_VERSION` → `app-v56`, `sw.js` CACHE → `chemowell-app-v56-6`. Tests: `test/v56-matcher.mjs` (headless, against the real corpus sliced out of `index.html`), `test/v56-helpbot.mjs` (133 assertions at 360 px and 390 px), plus v55 (145) and v52 (20). Spec: `outputs/DEV_BRIEF_v56_helpbot.md`. |
| app-v55 | 2026-08-10 | **Aaron's "chatbot", built: an offline Help centre with 117 step-by-step walkthroughs, replacing the 15-entry FAQ.** Aaron's original ask (REQUESTS.md, 2026-08-08): _"maybe it can't be something that you can respond to real time, but if not, it should have a very extensive list of problems it can walk a user through. much like the FAQ. but way more details to choose from… end to end app coverage."_ His own framing settled the technical question — there is no model, no API key and no backend in this app, and adding one would put a cancer diagnosis on someone else's server, which is precisely what we cancelled the sync work to avoid. So: a structured offline decision tree — **17 categories → 133 rows → an ordered walkthrough** — with one search box over the whole set, all shipped inside the same single-file app and fully available with the phone in aeroplane mode. Deliberately **not** a chat-shaped text input: a prompt that looks like a chatbot promises an answer to anything, and this can only answer what it was written for. Content authored first as `outputs/HELPBOT_CONTENT_v1.md` (1,966 lines) and transcribed into `HELP_TOPICS` **by script rather than by hand**, so the shipped copy is provably the reviewed copy. Each topic carries `cat/q/a/steps/branches/note/keywords/related`, extending the existing `FAQ_ITEMS` shape; the 15 FAQ entries survive untouched as the **Common questions** category, same accordion, same `faqOpenId` state, and the drawer has one Help row rather than two. `steps` is a real array rendered as an `<ol>` — the answer div has no `white-space:pre-line`, so a newline-joined string would have collapsed into a run-on paragraph. **The search-box bug that shipped four times before (v11/v22/v27/v45) is closed two ways:** the whole Help view is excluded from the 1s tick (nothing on it is time-dependent, so there is no reason to leave a second route back in) *and* the field restores its own caret after each re-render. The Auditor could not make it drop a character under any timing. **Nine medical-adjacent topics open with a care-team callout above the steps**, wording taken verbatim from the app's existing disclaimer — no thresholds, no numbers, no "this is fine", no dosing suggestion anywhere. **Auditor gate (`outputs/AUDIT_v55.md`, verdict "safe to ship after two copy fixes"):** all 133 rows opened and checked, 117/117 findable by their own question text, 117/117 Related chips exact, zero dangling ids, zero console errors, 0px horizontal overflow at 360px and 390px. Five findings, **all five fixed in this release**. **V55-1 (Medium)** — five Help/FAQ entries still told the user to *tap the name at the top of the menu*, which **app-v54 had just made non-interactive at Aaron's request**; the erase-everything walkthrough was pointing a caregiver at a dead element. This is the "fixed one of N call sites" failure for the fifth time, so the regression test now asserts the **absence of the phrasing anywhere in the file** rather than the presence of a fix at one site. **V55-2** — `helpRich()` rendered `**bold**` and `*italics*` but passed the spec's third markup form, `` `code` ``, through as literal characters, so the medication editor's most-reported sticking point told users to type a backtick; it now renders a real `<code>` chip (tinted, not monospace — a mono face at 14px reads as broken next to the body copy), and the test asserts no backtick survives into rendered text and that no chip can exceed its column. **V55-3** — the *calm* variant of the medical callout kept the heading "Contact your care team" over the standing not-medical-advice disclaimer, so a page explaining why a text box is greyed out opened by appearing to tell a frightened caregiver to phone the oncologist; the heading now follows the tone (`Not medical advice` for the five mechanics pages, `Contact your care team` kept for the four whose answer genuinely *is* "call now"). **V55-4** — a dead `state.view === 'faq'` router arm whose comment claimed it caught a persisted value; `VALID_VIEWS` has never listed it, so `restoreView()` rewrote it to `home` long before the router saw it. Deleted rather than left as a comforting no-op. **V55-5** — this README entry and the REQUESTS.md tick, which is why app-v54 appears below only now. Also logged, not fixed: **one oncology-nurse read of `sym-severe`** (index.html:2198) is worth buying before real users — it is the only place in the app that enumerates clinical signs, and the Auditor flagged it rather than guessing. **PM gate (`outputs/PM_v55.md`) returned NO-GO on the first fix pass, with five findings of its own — all five fixed, then re-verified to GO.** **PM-1 (Medium, blocking) was my V55-1 fix creating a fresh instance of V55-2:** it wrote `**Account**` into two FAQ answers, but FAQ answers were handed to the DOM raw rather than through `helpRich()`, so both rendered literal asterisks to the user. Same markup convention, second data structure, different renderer — the same failure shape as V55-1 itself, and my new regression assertion missed it because it was scoped to `HELP_TOPICS`. Fixed by routing FAQ answers through `helpRich()` rather than by stripping the two strings, so the two structures now share one convention and one renderer; the PM then round-tripped all 15 answers (rendered text == source with markup consumed, 15/15 at both widths) and confirmed only the 2 intended entries produce markup nodes. **PM-2, PM-6:** `release_check.sh` again. It fell back to a sentinel `"none"` when it could not parse `CACHE` out of `sw.js`, and a sentinel that can never equal anything is indistinguishable from a successful comparison — a reformatted or missing `sw.js` produced exit 0 *and the message "sw.js's CACHE constant changed with it"*, which is V53-4 through a different door. Unparseable is now fatal. The narrower fix for that (`head -1`) was itself wrong — with a commented-out old value *above* the real line it picks the comment and hard-fails a correctly bumped build — so parsing is now comment-aware and refuses to guess when more than one active definition exists. **PM-4:** this table said app-v54 shipped CACHE `chemowell-app-v54-1`; it shipped `v54-2`. A retroactive version-history row wrong about the one fact it exists to record is worse than no row. **PM-3 remains open and is logged in `BACKLOG.md`, not claimed as fixed:** the gate can prove `PUBLISHED.json` is self-*consistent*, never that it is *current*, so pushing and forgetting `./mark_published.sh` still reopens the stale-baseline hole. The new "N commits have changed index.html since that record" line is a standing disclaimer, not a detector — it prints identically on a correct run and on a dangerous one. Closing it properly needs something that can read `sw.js` from the deployed URL, which the post-push live-verify step already has a browser for. **Release mechanics, shipped alongside:** `PUBLISHED.json` + `mark_published.sh` replace `origin/main` as the gate's baseline, because in this sandbox `origin/main` is never fetched after a push and therefore points one release behind reality forever — which let a CACHE bumped in the *previous* release satisfy the gate on a build that never bumped it. Proved on scratch clones across 10 cases in both directions, including the exact reproduction that the old script passes green. `APP_VERSION` → `app-v55`, `sw.js` CACHE → `chemowell-app-v55-2`. Tests: `test/v55-help.mjs` (144 assertions), `test/v52-fixes.mjs` (20, two of which had quietly stopped guarding and were re-pinned), `test/audit-v55*.mjs` and `test/pm-v55*.mjs` (gate scripts), screenshots in `outputs/v55-*.png` and `outputs/v55fix-*.png`. |
| app-v54 | 2026-08-10 | **Aaron's report: the name at the top of the hamburger menu should be display-only.** Aaron: _"on the hamburger menu, when tapping the name at the top of the list…it takes you to the settings page. this shouldn't take you to any page as this should be visible only. there is a settings tab right underneath that thats you to the the profile tab. don't need both."_ The identity header (avatar, patient name, plan pill) was a `<button>` that navigated to Account — duplicating the Account row sitting directly beneath it, and giving the drawer two different-looking controls with one destination. It is now a plain `<div>`: not focusable, no pointer affordance, no navigation. The avatar initial is `aria-hidden` (it is decoration, and a screen reader announcing a stray letter before the name is noise), and the plan pill gained an explicit label — "Free plan" / "Pro plan" — because a bare "PRO" chip reads as an abbreviation out of context. **This also removed a focus stop from the drawer**, which is what led the Auditor to look at drawer focus handling properly and find a **pre-existing** defect dating to app-v22 — focus is never returned to the hamburger on close, and Tab can escape the drawer once focus lands on `<body>`. Confirmed identical on the live app-v53 build, so it is **not** a v54 regression; it is logged in `BACKLOG.md` for its own small release with its own gates, since it needs a render-order fix rather than a copy change. Affects keyboard and switch-access users only, which is why it went unnoticed for 32 releases. `APP_VERSION` → `app-v54`, `sw.js` CACHE → `chemowell-app-v54-2`. **Note on this entry:** it was written retroactively as part of app-v55 (Auditor finding V55-5) — app-v54 shipped without its README row, which TEAM.md's release checklist requires before push. |
| app-v53 | 2026-08-10 | **Aaron's request — treatment type is now editable in Settings — plus the release safety script that was quietly not working.** **Treatment type editable (Aaron: _"there should be a way for people to update their treatment as you described. they could go from chemo to radiation. needs fixed in setting"_).** Settings gains a **Treatment** section offering Chemo / Radiation / Both / Other with the caption "Change this if your treatment plan changes — moving from chemo to radiation, for example. Your medications, doses and history are all kept." Previously the answer was asked once on the welcome screen and could then only be changed by the legacy "Finish setting up this profile" card, which only appears when `sex` or `treatmentType` is *missing* — so a wrong pick, or the very common chemo → radiation plan change, could only be corrected by erasing the profile and losing all history. Treatment type drives which Home cards render, which medication options exist, and copy across several screens, so this was not cosmetic. Switching **to Other** normalises stranded treatment-day restrictions to `treatmentMode:'none' / treatmentOnly:false` in storage, because app-v52 removed that control entirely for Other — leaving the value stored would mean a medication-availability rule the user can neither see nor undo, which would silently reactivate on the way back. **V53-2 (Auditor, Medium, functional) — the same "fixed one of N call sites" mistake as V52-1, caught again.** `treatmentType` has two write sites; the first build normalised only the new Settings control and left the legacy setup card untouched. That was inert while a profile stranded on Other had no way back — and v53's own new control *is* the way back, so saving as Other through the legacy card left an orphaned rule that reactivated silently. Both sites now route through one shared `applyTreatmentTypeChange()`. **V53-1 (Auditor, Medium, copy)** — the Other-switch toast counted `excluded`-mode medications and then told the caregiver they were "no longer limited to treatment days", which is the opposite of what an excluded rule does, and never confirmed the type had changed at all; it now reads "Treatment set to Other — treatment-day rules removed from N medications". **`release_check.sh` actually works now.** The mandatory pre-push gate diffed against `HEAD`, i.e. *uncommitted* work only — but APP_CLAUDE.md rule 8 says commit early and often and pushes here are manual web uploads of already-committed files, so by the time anyone ran it the risky change was in a commit and it printed "✅ No index.html changes pending" and exited 0 on the exact index-without-cache-bump failure it exists to block. It now diffs against `origin/main`, compares the extracted `CACHE = '…'` constant itself rather than "is sw.js non-empty in the diff" (V53-4 — a cosmetic sw.js edit used to satisfy it, and it then printed the untrue "CACHE constant changed with it"), and is committed executable so TEAM.md's documented `./release_check.sh` no longer exits 126 (V53-5). **FAQ fix:** "Can I start over / erase everything?" pointed at Settings; Start over lives in **Account**, and the answer now says so. **Gates:** `outputs/AUDIT_v53.md` — 242 assertions, 26 profiles from wiped installs, 12 switch transitions, a day-by-day treatment-window walk — blocked the first build on V53-2. `outputs/PM_v53.md` independently re-verified the fixed build rather than accepting the audit of the pre-fix one: the Other round-trip confirmed in `localStorage` and across a reload, the legacy-card path confirmed to still save `sex` and to normalise only on Other, all four transitions confirmed lossless, app-v52's H-1/H-2/H-3/D-2/D-3/D-4 fixes confirmed still holding (window walk exact at both edges of both modes, banner 5 == History "Missed Doses (5)", CSV `8 mg` / `2 tabs` / `2 applications` with zero invented "pill"), zero console errors, 360-390px clean, and `release_check.sh` proved to fail in both directions and pass when correct on scratch clones. The PM also found and logged a **new** hole the audit had judged safe: a *stale* `origin/main` — this sandbox's normal state after every web upload, since it has no network to fetch — lets a CACHE bumped in the *previous* release satisfy the check, so the guard weakens one release after install. Logged at the top of `BACKLOG.md` as blocking the next release; it does not affect app-v53, whose base ref genuinely was the live build. `APP_VERSION` → `app-v53`, `sw.js` CACHE → `chemowell-app-v53-2`. |
| app-v52 | 2026-08-09 | **Three real defects found by a full 82-case Zero Day Auditor sweep of app-v51 (`outputs/AUDIT_full_app_v51.md`), plus an Aaron-requested product change.** **H-1 (safety, High) — a missed-dose alert that could never be cleared.** Home's red banner counted missed doses across every calendar day since install (`pastMissedCount`), but History only ever built its day list from days that had a logged entry. A day with nothing logged — exactly the day most likely to contain a real missed dose — could therefore never render a missed row, so Home said "2 missed doses from previous days", the Review screen said "you're all caught up", the "Clear all" button never appeared (it is gated on `totalMissed > 0`), and a reload changed nothing. History now enumerates the same day range the banner does and seeds any day that has misses (days without misses are not added, so no blank filler rows appear). An un-dismissable red alert trains people to ignore red alerts, which is why this was treated as safety-relevant rather than cosmetic. **H-2 (High) — a medication could vanish from Home permanently.** A medication set to "Only near treatment day" was hidden whenever `treatmentActiveOn()` returned false — but that function returns false both for "outside the window" and for "there is no treatment date at all", which are not the same question. With no date set, the medication disappeared from Home with no message and no way to bring it back, and radiation-only profiles had the Treatment schedule card *and* its Settings toggle both hard-hidden, so they had no way anywhere in the app to set a date while the medication editor still happily offered the restriction. Fixed with a new `hasTreatmentDate()` predicate and a single `treatmentOnlyBlocks()` gate that all ten call sites now route through (the first attempt guarded one of seven, which is why this was re-audited); the Treatment schedule card is now a *default*-off preference for radiation-only rather than a hard block, so those profiles keep a clean Home but can turn it on. **H-3 (High) — CSV export and the printable doctor's report invented a pill count.** `e.pills` is not a pill count: `parseDoseOptions` stores any leading number from a free-text dosage label there, so "500 mg" produced "500 mg, 500 pills" (Aaron's own report), "8 mg" produced "8 mg, 8 pills", and "2 applications" produced "2 applications, 2 pills" — a fabricated unit the user never chose, printed twice, in the document a clinician reads. The dose label is now printed as the amount and nothing else; the raw number is used only when there is no label at all, and then with the medication's real unit. **Aaron's product change:** near-treatment / exclude is gone entirely from the medication editor for "Other" profiles ("the whole near treatment and exclude shouldn't be there at all for them") — picker, the days-before/after fields, the "Active window" summary and the Meds-list chips. Restrictions saved by an earlier build are made inert at runtime rather than stranded in a state the editor can no longer show or undo. **Designer pass (`outputs/DESIGN_v52.md`), four must-fixes:** a shown-with-no-date medication now carries an amber "No date set" chip and the caption "Showing every day until you set a treatment date." instead of a bright-green "Available" that says nothing (green means "go" everywhere else on Home); on radiation-only profiles Radiation sessions now ranks above the opt-in Treatment schedule card; a History day seeded purely because it has misses reads "Nothing logged · 2 MISSED" instead of "0 doses · 2 MISSED". **Gates:** Auditor rejected the first build (`outputs/AUDIT_v52.md`, five findings including a medication that read "Available" on its own card and "Outside its treatment-day window" in a Morning group on the same screen); re-gate cleared it with one Medium open (`outputs/AUDIT_v52_2.md`, 95 cases, 5 profiles, 24 medications); that Medium (orphaned Days-before/after controls surviving on an *upgraded* Other profile) was fixed and the PM independently reproduced it via a real v51→v52 upgrade path before signing off (`outputs/PM_v52.md`). Independent PM verification of the headline claims: treatment-window walk exact at −1/0/+1 with no off-by-one, banner-vs-History agreement exact at both 9 and 1,460 missed doses, CSV and printable report clean across mg / non-mg / no-numeric-label doses, zero console errors. `APP_VERSION` → `app-v52`, `sw.js` CACHE → `chemowell-app-v52-4`. |
| sync-backend (no app version change) | 2026-08-09 | **The sync relay is provisioned, live, independently audited, and hardened — the multi-device sync blocker is cleared.** No change to `index.html` or `sw.js`, so nothing about the shipped app changed and no APK rebuild is needed; `SYNC_API_BASE` is still `''` and the app has never called this backend. **Provisioning:** the three Vercel dashboard steps that had been sitting on Aaron were done directly in his browser via Claude-in-Chrome — project `chemowell-sync` created (root dir `sync-backend`, auto-deploys from `main`), the Vercel Authentication login wall disabled so the API is publicly reachable, and a Blob store connected. This sandbox's Vercel API token can deploy code but cannot create projects or change project settings, which is exactly why these three steps had been treated as Aaron's to do; driving the real dashboard in a browser removed that constraint entirely. The store was created **Private**, not public: the payload is already end-to-end ciphertext, but pathnames here are deliberately stable and non-random (required for the compare-and-swap), so a public store would make "the ciphertext is only as private as a URL" the actual guarantee. Verified by test rather than by the dashboard label — an unauthenticated GET of a real blob URL returns 403. **Audit:** the mandatory Zero Day Auditor gate (`outputs/AUDIT_sync_backend_provisioning.md`; ~1,400 real requests against the live deployment, plus two real 10-minute expiry waits and a 1,131-record scale test) found **2 Critical and 6 High** issues in the relay as built in app-v49. The two Critical ones chained into a working attack: an unvalidated `recordId` let a push escape the `profile/` namespace, and a deliberately-wrong `baseVersion` made the conflict response echo the whole stored object — an unauthenticated read oracle that returned other users' pairing sessions *including their live 6-digit codes* — while the code itself was brute-forceable inside its own 10-minute window (270 req/s measured, no rate limiter; the original design justified a 6-digit code by assuming a limiter that was never built). Also High: `pull` never paged past Vercel Blob's 1,000-record default and returned HTTP 200 with a silently short list — on a medication list that means a caregiver sees a plausible-looking list with entries missing and nothing says so, the same fails-silently shape as the app-v47→v50 export regression; ~30-37% of losing concurrent writers got a 500 instead of the 409-with-current the client needs, so their edit was dropped while they were told the server had failed; anyone holding a `profileToken` could destroy any record (encryption prevents disclosure, not destruction); and security-relevant ids came from `Math.random()`. **Fixes:** strict `^[A-Za-z0-9_-]{1,64}$` validation on every id at the edge plus whitelisted conflict responses; pairing codes moved to 10 chars of Crockford base32 (~50 bits) so strength lives in the secret rather than in a limiter serverless can't honestly guarantee, with a best-effort in-memory limiter as defense in depth (measured: engages at request 31 of 30/min on a warm instance); cursor-based paging with an explicit `truncated` flag; both of Vercel Blob's conditional-write failure shapes now caught; a write token minted at first share (server stores only its SHA-256, and the joining device receives its copy inside the encrypted wrapped-key payload, so it never crosses the server readably and is not derived from K); `node:crypto` randomness throughout; the `newVersion === baseVersion + 1` invariant enforced; expiry enforced on all three pairing endpoints with expired material deleted on touch. Re-verified live: **33/33**, including replaying both Critical attacks and confirming 0 500s across 36 concurrent pushes. Two contract changes the pairing UI must build against are recorded in REQUESTS.md. |
| app-v51 | 2026-08-08 | **Fixed a real, currently-live console error that was silently breaking the native CSV/PDF export share-sheet feature (app-v47).** Found by a retroactive Zero Day Auditor pass on app-v50 (run after-the-fact per TEAM.md's mandatory-gate rule — see that entry below for why it ran late). `@capacitor/filesystem@8.1.2`'s own published CDN bundle ends with `})({}, capacitorExports, synapse);`, referencing a bare `synapse` global a plain `<script>` include never provides — this threw `ReferenceError: synapse is not defined` on every single page load (live-confirmed, 6/6 reproductions) and silently kept `Filesystem` out of `window.Capacitor.Plugins` entirely. Since the exact same `index.html` is what the native Android APK's WebView loads, this very plausibly meant the app-v47 fix (CSV/PDF export handing off to Android's real share sheet) had silently regressed back to going nowhere — failing closed via the app's own null-safe fallback, so no crash and no visible error, just quietly not working. The fix was already researched and pre-documented in `BACKLOG.md` before app-v47 shipped but never applied: a one-line no-op shim (`window.synapse = { exposeSynapse: function(){} }`) loaded before the filesystem script tag, safe because `exposeSynapse()` only wires up a rarely-used synchronous native-bridge proxy that the app's actual `Filesystem.writeFile()`/`Share.share()` calls never touch. Verified: the console error is gone on a fresh regression run (0 console errors), the shim is present and callable. The native half (does `Filesystem` now actually register inside the real APK, does export/share now actually work again) still needs a fresh APK build + Aaron's on-device confirmation, same as the app-v50 native items below — tracked in REQUESTS.md, not claimed as done. `sw.js`'s cache version bumped to match. |
| app-v50 | 2026-08-08 | **Reminder reliability: battery-optimization exemption + automatic early-scheduling buffer when exact alarms aren't granted.** Researched Aaron's exact-alarm-toggle report against his confirmed device (Samsung Galaxy S25 Ultra, One UI 8.5, Android 16): the permission ChemoWell requests is genuinely available to any app (not reserved for clock/calendar apps — that restriction applies to a different, unrelated permission) and works on most phones; what Aaron hit matches a documented Samsung One UI quirk where the toggle can misbehave or the whole settings section can disappear after being denied once. Kept the control (it's real and worth using) and shipped three fixes: (1) a "?" explainer on the exact-timing Settings card covering this, with the standard fix to try (force-stop/reopen ChemoWell, or Settings → Apps → ⋮ → Reset app preferences); (2) reminders now automatically arm 4 minutes ahead of their real time whenever exact alarms aren't confirmed granted, so ordinary Android scheduling delay lands early instead of late — fully automatic, no user action, every displayed time stays the real one, and it reverts to exact-as-configured the moment exact alarms are confirmed on; (3) a new, independent "Allow background activity" control (`@capawesome-team/capacitor-android-battery-optimization`) for the separate battery-optimization restriction, which can delay delivery even with exact alarms granted — offered only when there's an actual fix to apply. `.github/workflows/android-build.yml` updated to inject the plugin's required manifest permission into the freshly-generated native project (same pattern as its existing icon-copy step, since `android/` isn't committed). Verified in this sandbox: web build zero regressions (Playwright, 0 console errors across a full setup-flow walkthrough), the new scheduling-buffer function unit-verified for all three exact-alarm states including its never-pad-into-the-past floor guard. The native half (manifest injection, real plugin/OS behavior) depends on this project's CI build + emulator smoke test and, beyond that, Aaron's own device — noted in REQUESTS.md rather than claimed as verified. `sw.js`'s cache version bumped to match. **Process note added 2026-08-08:** this release shipped self-verified by the Lead Developer only — the Zero Day Auditor and Project Manager gates TEAM.md requires for every release ("no exceptions") were skipped. Caught the same day when Aaron flagged that the team process wasn't being used; both gates were run retroactively (`outputs/AUDIT_v50.md`, `outputs/PM_GATE_v50.md`) and returned a clean PASS/GO on this release's own diff, but the Auditor's live-testing pass turned up a real, separate, currently-live defect (see app-v51 above) that had been sitting undetected since app-v47 specifically because this kind of live gate hadn't been run in a while. CI run #53 (the workflow fix) also independently passed both jobs (build + emulator-smoke, 5m24s) before the retroactive audit started. |
| app-v49 | 2026-08-08 | **Foundation work for multi-device/multi-caregiver sync (in progress, not yet user-facing).** Per the confirmed architecture (`outputs/SYNC_DEVELOPER_BRIEF_v2.md` — end-to-end zero-knowledge encryption, server never holds a readable copy of patient data), this release adds the on-device building blocks: a stable per-install device id, per-profile encrypted-sync-key storage, and a Web Crypto module (`generateSyncKey`, `encryptRecord`/`decryptRecord`, and the full ECDH+HKDF pairing-handshake functions) — all pure functions, fully additive, not called from anywhere in the existing UI yet. Verified with 9/9 automated checks in Node (encrypt/decrypt round trip correct, decrypting with the wrong key fails closed rather than returning garbage, a simulated two-device pairing handshake produces the exact same key on both sides, and a simulated eavesdropper with a different keypair cannot recover it) and re-confirmed against the real browser's `crypto.subtle` implementation via Playwright — same 4/4 checks passing there too, plus a full setup-flow regression walkthrough (new profile → Home) with zero console errors, confirming this is a true no-op for every existing feature. Also stood up the actual backend that will receive this data: a separate `sync-backend/` Vercel project implementing the pairing handshake and encrypted push/pull endpoints, using Vercel Blob's real ETag-based conditional write as the atomic version-conflict check the design calls for — deployed and build-verified, but not yet reachable by the app (`SYNC_API_BASE` is still empty, and the actual pairing/sync UI hasn't been built yet) pending three one-time setup steps in Aaron's Vercel dashboard (new project, disabling deployment-protection's login wall on a public API, connecting Blob storage) that this environment's Vercel access can't perform on its own. `sw.js`'s cache version bumped to match, though this release has no visible effect on the app as installed. |
| app-v48 | 2026-08-08 | **Onboarding tour now auto-navigates to the tab it's describing, and no longer blocks the background.** Aaron: "I can't even click on anything during tour after adding med and going back home. after that, can't click on reports, inpatient or symptoms. this is why it just needs to take you there automatically when it talks about each tab. the user can go back to those tabs on their own time." Root cause: the tour's four trailing informational steps (Logging doses, Reports, In-Patient, Symptoms) only ever highlighted the relevant bottom-nav icon while leaving the user parked on Home, and rendered a full-screen invisible overlay (`tour-scrim`, a deliberate v28 anti-sidetracking measure) that silently swallowed every tap and scroll underneath — the exact "can't click on anything" Aaron hit. Fixed: those four steps now carry a `navTarget` and `setTourStep()` calls the app's real navigation the instant each becomes current, both stepping forward and stepping back, so the tour shows the actual Reports/In-Patient/Symptoms page instead of just a glowing icon; the scrim no longer intercepts input (kept as a purely visual dim), so the real page underneath is fully usable while the guide card is up. **Investigated Aaron's report that the medication editor still shows "near treatment"/"exclude near treatment" wording for a chemo/radiation-free ("Other") profile during the tour** — read the actual current source (`isOtherTreatmentType()`, `treatmentModeOptions()`, and every call site: the radiogroup, its field label, the days-before/after fields, the med-card badges, the Home card caption) and confirmed it is already fully adaptive and reads "your date," not "treatment," exactly as shipped in app-v42 — live-verified fresh with a real "Other" profile walked through the real onboarding + tour flow, zero non-adaptive wording found anywhere. Most likely explanation for Aaron seeing the old copy: a stale service-worker cache on his device (this exact failure class has hit this app before — see the app-v40-era fix `release_check.sh` now guards against). No code change made here since none was needed; asked Aaron to fully close/reopen (or reinstall) the app and check the version footer in Settings against app-v48. Verified live via Playwright end-to-end through the real onboarding + tour flow (fresh profile, real Meds→Add→Save, real nav taps, Back/Next in both directions) — 9/9 checks passing, plus the existing "Other" wording check re-confirmed (3/3) and the full regression suite (9/9), zero console errors throughout. `sw.js`'s cache version bumped to match. **Follow-up, same day:** chasing exactly why Aaron might still see stale wording turned up a real, previously-unknown bug in `sw.js` itself, confirmed by inspecting the live Cache Storage contents directly on the deployed site — its install step was cache-first and could permanently bake a stale `index.html` into an otherwise correctly-versioned new cache, from either a brief GitHub Pages CDN edge-propagation inconsistency right after a push, or (confirmed the more persistent culprit) the browser's own ordinary HTTP cache silently substituting a disk-cached response for what should have been a real network fetch. Fixed in two follow-up commits: the document itself now fetches network-first with cache used only as an offline fallback, and every shell fetch explicitly forces `{cache:'reload'}` to bypass the browser's HTTP cache. Verified live by inspecting Cache Storage before and after — confirmed the stale bake-in happening in real time, then confirmed it's gone post-fix. This is very plausibly the real explanation behind more than one past "why hasn't my fix reached the device" report, not just plain stale-registration. |
| app-v47 | 2026-08-08 | **Bridged CSV export and the printable report to Android's real native share sheet.** Aaron: "export says check downloads but I don't see anything in my notifications bar saying anything was downloaded... is this bc it's only an APK or bc it's wrapped around an HTML?" Root cause: both features were built as plain browser tricks — a blob `<a download>` click for CSV, `window.open()` + `.print()` for the report — neither of which reaches Android's real file system or print pipeline from inside the Capacitor native WebView (the same underlying gap on both, not two separate bugs). Added `@capacitor/filesystem` and `@capacitor/share`; on the native build both features now write the file and hand it to Android's real native share sheet (same UX as any other Android app's file export), letting Aaron pick where it goes — Drive, email, a printer app, Files. The web/PWA build's behavior is completely unchanged (falls straight through when not running natively). **This is a native change, not a content update** — it needs a freshly built APK reinstalled, not just the app reopened; the push to `main` triggers that build the same way past native changes did. Verified on the web build (unchanged behavior, no regressions); the native path itself needs Aaron's confirmation once the new APK is installed, since this sandbox has no way to test the real native share sheet. `sw.js`'s cache version bumped to match. |
| app-v46 | 2026-08-08 | **Fixed Dosage options permanently locking Daily limit for non-mg medications.** Aaron: "Dosage option still waits for 'mg' if Limit Unit is set for Number of applications. should it still wait for 'mg'? the dosage option banner pops up and can't be bypassed." Root cause: the field required a specific whitelist word (pill/tab/tablet/cap/capsule/application) right after the number for a non-mg entry to count, but the field's own helpIcon example — "1 patch, 2 patches" — doesn't use any of those words, so following the app's own suggested format could permanently fail the check and lock Daily limit with the warning showing and no way out. Fixed by counting any leading number in a Dosage options entry, regardless of the word after it. Doesn't change which units the Limit unit dropdown offers (that's a separate, larger tracked item) — fixes the parsing bug trapping people using the units that already exist. Verified live end-to-end: "1 patch, 2 patches" with Limit unit "Number of applications" now unlocks Daily limit, saves cleanly, and the existing mg-unit path is unchanged. `sw.js`'s cache version bumped to match. |
| app-v45 | 2026-08-08 | **Fixed the permanent-delete screen flicker, and confirmed/strengthened "can't be undone" language on every delete flow.** Aaron: "when trying to permanently delete things, the screen keeps refreshing constantly. when I do click erase, I mentioned before there should be another pop up to say that this is permanent and can't be undone." Root cause of the flicker: the app's 1-second render tick-loop skips rebuilding the screen while certain modal/confirm states are open, and that skip-list was missing the Erase-all modal and every tap-to-arm delete-confirm state (medication, appointment, note, profile) — so the whole screen was torn down and rebuilt every second while any of them was open, which read as constant flickering (the same root-cause pattern as three earlier bugs in this file — v11, v22, v27). Fixed by adding all of them to the skip-list. Separately confirmed the "Erase all data" flow already carries real "this will permanently delete everything... there is no way to bring it back" language (shipped app-v40) — if it's still not showing on Aaron's device, the installed APK is likely serving a stale cached build. Also added explicit "This can't be undone" wording to the medication, appointment, and note delete-confirm flows (and strengthened the profile one), which previously lacked it. Verified live: each confirm dialog's DOM node now stays stable across multiple 1-second ticks (not rebuilt), and the new wording renders in every flow. `sw.js`'s cache version bumped to match. |
| app-v44 | 2026-08-08 | **Restructured the Pro tier's pricing-card bullets.** Aaron: "there still needs to be a real gap between plus and pro. i'm still not seeing it. still need to remove priority access to new features unless you tell me what you envision with that." Dropped "Priority access to new features as they launch" entirely — no concrete deliverable ever backed that claim. Plus's backup bullet now reads "Backup & restore — move your data to a new phone," explicitly a single-device continuity feature; Pro's bullet now reads "Complete data export — entries, appointments, and notes bundled into one shareable file for your care team or another caregiver," explicitly a different, sharing-oriented feature — giving Plus and Pro a real functional gap instead of overlapping backup language. Bullet order: leads with real-time shared caregiver access (the one benefit Aaron said would actually sell him), then the export and trend-insights bullets he confirmed building, with unlimited profiles last per his own suggested ordering. `sw.js`'s cache version bumped to match. |
| app-v43 | 2026-08-08 | **Lightened the Sunset Glass background gradient** — Aaron reported the page background (behind every card, on both the onboarding screen and the main app) read as too dark. Lightened all three gradient stops by 14% lightness in HSL space, keeping the same hue progression: `#FF5F6D → #FFA6AE`, `#FF9A44 → #FFC18B`, `#FFC371 → #FFE1B8`. The header and bottom nav bar sit on their own separate light background and were unaffected. **Added an on-screen explainer to the In-Patient tab** — it previously had no on-screen explanation of what it does (only a separate FAQ entry); added a "?" helpIcon next to "In-Patient Status" describing the Start/End flow, the Home-logging pause while active, and the "+" button for logging a forgotten past stay. `sw.js`'s cache version bumped to match. |
| app-v42 | 2026-08-08 | **Adaptive "Treatment day" wording for non-chemo/radiation profiles** — Aaron: "I could use this app for my own meds...but I don't have treatment days aside dr visits." Every user-facing "treatment day" string (the medication editor's availability section and its radio options, Home card captions, the treatment-plan banner, the FAQ entry) now reads as "your date" for a profile whose onboarding treatment type is "Other"; chemo and radiation profiles keep the exact original copy, unchanged. Verified live for both profile types, including confirming zero regression in the unchanged chemo/radiation wording. **Reordered the medication editor**: "Minimum gap between doses" (scheduled meds) / "Hours between doses" (as-needed meds) moved from the end of the form up next to Dosage options and Daily limit, where the related dosing questions live; the scheduled variant's label now explicitly says "(hours, optional)." `sw.js`'s cache version bumped to match. |
| app-v41 | 2026-08-08 | **Fixed: medication dose reminders silently never fired between 10 PM and 8 AM ("quiet hours")** — found live by Aaron on the installed APK (scheduled a 10:30 PM dose, closed the app, got nothing). Root cause: both the in-app reactive reminder check and the closed-app native pre-scheduled alarm path silently skipped any dose window opening in that range, with no warning anywhere that the chosen time would never notify. Appointment reminders were always deliberately exempt from this same rule (a caregiver is meant to be woken for a time-sensitive appointment); a medication dose is at least as time-sensitive, so at Aaron's direction the quiet-hours rule is removed for dose reminders entirely — they now fire at any hour, matching appointments. The daily check-in nudge (a non-critical, once-a-day prompt, not medication-safety logic) intentionally keeps its own separate 8 AM–10 PM window, unchanged. Verified live: a 10:30 PM and a 3 AM dose window both now correctly fire a reminder end-to-end through the real notification pipeline (mocked device clock, not a simulated date, since simulated dates deliberately suppress reminders for a separate reason). Also added a "?" explainer to the medication editor's **Dosage options** field, clarifying what the comma between multiple strengths (e.g. "500 mg, 1000 mg") means and that they share one schedule/limit — Aaron confirmed multiple strengths is intentional, the wording around it was just unclear. `sw.js`'s cache version bumped to match, so installed copies pick this up automatically. |
| app-v35 | 2026-08-06 | **Pre-scheduled native notifications** — dose and appointment reminders are now scheduled directly with Android's AlarmManager (via `@capacitor/local-notifications`) up to 72 hours ahead, so they arrive even while the app is closed or the phone is locked; the reactive (foreground-only) reminder path is disabled on native builds (v35 fully replaces it there) and unchanged on web/PWA. Settings shows a live status card with 8 distinct, mutually exclusive states (checking, not-yet-asked, on, on-but-exact-alarms-denied, blocked, sync-failed, paused-while-simulating-a-date, nothing-due). This is a clean reimplementation, not a byte-for-byte restore: the feature was lost twice to cloud-session disk resets before reaching GitHub, so this build was written fresh against the last verified `app-v34` base and re-verified from scratch, with commits pushed incrementally throughout to bound any future reset to minutes of work rather than the whole feature. Ran the Owner's full Quality Chain (Developer → Lead Developer → Designer → Lead Designer → Auditor → Lead Auditor → PM), which caught and fixed two real defects before release: a permanently-disabled (but visually normal-looking) "turn on notifications"/"retry" button caused by an HTML boolean-attribute trap, and three medication-mutation paths (delete, pause, edit) that didn't trigger a re-sync, leaving stale alarms armed for a deleted or paused medication. Also fixed: a possible duplicate reminder if the app was foregrounded at the exact instant a pre-armed dose window opened, and the 6-hour safety-net resync being skippable by its own change-detection guard. Two narrow, self-healing gaps were found and knowingly left open (undoing a logged dose, and deleting an inactive profile, don't immediately re-sync — both correct themselves within the same 6-hour safety net). `sw.js`'s cache version bumped to match, so browsers with the prior version cached will pick up this update. |
| app-v34 | 2026-08-05 | **Intended-use positioning: patients & personal caregivers only** (Owner-directed). ChemoWell now states clearly, in three places, that it is for patients and their personal/family caregivers — and explicitly NOT intended for doctors, nurses, or other healthcare professionals, or for hospitals, clinics, or any healthcare facility (the Owner wants zero ambiguity and zero HIPAA exposure). (1) New FAQ item "Who is ChemoWell for?" pinned as the first question. (2) New FAQ item "I'm caring for more than one person — can I track them all?" placed with the profile questions — confirms multi-patient personal caregiving is supported via profiles (Free 1 / Plus 3 / Pro unlimited, verified against tierLimit). (3) New "Intended use" paragraph in Settings → About & legal, above the Medical disclaimer, in the same style. Also fixed a contradiction the Developer stage caught: the Pro plan card advertised "Built for professional and multi-patient caregivers" — "professional" directly undercut the new positioning and is now "Built for caregivers looking after several people". The word "nurse" appears in the app only inside the two not-intended-for sentences. |
| app-v33 | 2026-08-02 | **Radiation support + profile buildup + Account + export** (Owner-directed redesign). (1) Onboarding is now a short 3-question profile buildup: patient name, Male/Female, and treatment type (Chemo / Radiation / Both) — one card, no wizard. (2) Menstrual-cycle tracking (which "Cycle" always was — corrected from the earlier mistaken chemo-cycle reading) is now Female-only: the toggle, Home banner, and Cycle report are hidden for Male profiles. (3) Radiation patients get a real home: a Radiation Sessions card ("Session N of M", derived-not-stored numbering, optional planned-total with progress bar, one-tap "Log today's session", backdating via the shared time modal) plus a Radiation report; radiation-only profiles no longer see the chemo plan banner or treatment-date card. Skin-reaction symptoms gain optional severity (Mild/Moderate/Severe) and body-site fields. (4) New Account view in the drawer: current plan, all profiles with treatment type + entry counts, profile switching, and export. (5) Export (Owner decision): CSV of everything is FREE for all tiers; the formatted printable doctor's report (print → save as PDF) is the Plus/Pro perk — Plans sheet copy updated to match. (6) Pre-v33 profiles get a non-dismissible "Finish setting up this profile" card on Home until sex + treatment are answered; until then everything behaves exactly as before. New profiles from "Add profile" now route through the setup screen (name prefilled) instead of skipping it. |
| app-v32 | 2026-07-30 | Ships the "leave a tip" prompt with corrected, honest copy — the actual working version of what was intended to ship as app-v30/app-v31. **What happened:** app-v29 shipped the prompt framed as "help keep the app free and ad-free," which the Owner correctly flagged as false and contradictory (ChemoWell already has paid Free/Plus/Pro tiers) — see the app-v29 entry below. A revert was built and committed locally as "app-v30," then a corrected-copy version as "app-v31" — but between finishing that work and pushing it, this environment's sandbox reset the local working copy back to the last-pushed state (a known instability in this setup), silently discarding both commits before the push happened. The push still went through and even carried the commit message "app-v31: revert v29 donation banner, re-add with honest copy," but its actual file contents were unchanged from app-v29 — an empty, mislabeled commit now sitting in this repo's history. Caught by re-fetching and diffing the pushed commit against what was intended, rather than trusting the commit message. This entry (app-v32) is the real fix, built fresh and verified as unchanged in the working tree immediately before this push. **The actual fix:** same non-intrusive mechanics as app-v29 (dismissible card on Home, shown at most once every 20 logged entries, never a modal, permanent opt-out via "Don't ask again") but the copy no longer makes any claim about the app being free, ad-free, or needing support to keep running — framed purely as an optional, appreciative "if this helped, here's a way to say thanks," entirely unrelated to the paid tiers. Button relabeled from "Support ChemoWell" to "Send a gift." Still gated fully inert — the banner cannot render at all — until `SUPPORT_LINK` is swapped for the Owner's real donation link (placeholder unchanged: `ko-fi.com/REPLACE_WITH_REAL_LINK`). **Verification:** the same 21-check Playwright suite from app-v29 (re-pointed at the new copy/button text), run once with a temporary stand-in "ready" link to prove the feature works, then reverted and re-verified fully gated off with the real placeholder in place — plus a full regression re-run of all three prior release suites (38 checks). 59 checks total, zero console errors. File contents re-confirmed on disk immediately before this push (the specific step that was skipped last time). Live-verified on the deployed site after clearing the service-worker cache. |
| app-v29 | 2026-07-29 | Owner-requested: an optional "support the app" prompt — the only monetization touchpoint anywhere in ChemoWell, there are still zero ads. A dismissible card (never a modal, never blocks anything) shows on Home at most once every 20 logged entries (any medication dose, vital, or symptom — anything that writes through the shared `addEntryDB` path), offering an optional one-time gift framed around keeping the app free and ad-free. Three ways to dismiss it (the donate button itself, "Maybe later", or the × ) all just push the next appearance out another 20 logs; a separate "Don't ask again" link turns it off permanently. All state (`supportPromptAt`, `supportOptOut`) persists per-profile the same way the existing missed-dose-banner dismissal does. **Safety gate:** the donation destination (`SUPPORT_LINK`) is still a placeholder pending the Owner's real Ko-fi/Stripe link — rather than ship a button that goes nowhere, a derived `SUPPORT_LINK_READY` check (true only once the URL no longer contains the word "REPLACE") keeps the entire banner from ever rendering until a real link is swapped in. Nothing else about the app changed; this is purely additive and fully inert until the Owner provides a real link. **Verification:** self-tested directly — a dedicated Playwright suite (21 checks) covering the 20-log threshold timing (19 vs. 20 vs. 39 vs. 40 entries), all four dismiss paths, persistence across reloads, permanent opt-out sticking past further logs, and the donate button actually opening the link, run once with a temporary stand-in "ready" link to prove the feature works, then reverted and re-verified gated fully off (banner and button absent from the DOM even at 100 logged entries) with the real placeholder back in place. Full regression re-run of all three prior release suites (38 checks) confirmed zero unrelated breakage. 59+ checks total, zero console errors. Live-verified on the deployed site after clearing the service-worker cache. |
| app-v28 | 2026-07-29 | Owner-reported, a batch of onboarding/tour and editor issues found while walking through app-v27 live, fixed in one fast-turnaround pass. **(1) Keyboard-open scroll jump:** root cause was more specific than app-v27's fix addressed — tapping directly INTO a text field (most noticeably Medication name right after onboarding) opens the on-screen keyboard, which resizes the viewport and can leave the field scrolled out of view, forcing a manual scroll back up. Added a global focus listener that nudges the focused field into view once the keyboard's own open animation has settled. **(2) Dosage options keyboard resetting mid-type:** a real, confirmed bug — typing "500 mg" kept losing the numeric keyboard page back to the default alphabet page after every character. Root cause: fields with a live preview rebuild the whole editor on every keystroke, which blurs and refocuses the input even though JS-perceived focus is restored — on a real phone that native blur/refocus resets the on-screen keyboard's current page every time, visible only on fields mixing letters and digits (Daily limit's pure-numeric keypad has no page to reset to, which is why the Owner correctly noted it "works fine"). Fixed by debouncing the rebuild for Dosage options and Daily limit to ~450ms after typing pauses — the live preview still updates, just slightly after the last keystroke instead of on every one, and the keyboard now only resets between words instead of between characters. **(3) "Extra hours between doses" still unclear:** relabeled to "Minimum gap between doses (optional)" with copy leading "on top of the reminder times above," and given its own "?" help icon (same pattern as Schedule windows) with a worked example showing exactly how it interacts with the reminder windows. **(4) Tour popup landing away from the newly-added medication:** root-caused to a regression from app-v27's own scroll-jump fix — the tour's guided positioning (which deliberately scrolls to center the newly-added medication card under its popup) was being silently undone a frame later by app-v27's deferred safety-net scroll-to-top, since that fix didn't know anything had scrolled on purpose in between. Added a generation-token check so a later, more-authoritative scroll (like the tour's) cancels a still-pending deferred reset from an earlier one. **(5) Reports tab back button unreachable after tapping page content:** could not be reproduced directly in the test environment across several plausible flows (Missed-doses toggle, entry remove/confirm, missed-dose Took-later/Skipped/Clear); hardened defensively regardless — raised its z-index above the bottom nav bar and increased its clearance from it, in case a device with a larger safe-area inset than assumed here was letting the nav bar's top edge overlap and win the tap. Flagged to the Owner as unconfirmed; asked for the exact button tapped beforehand if it recurs. **(6) Tour lets users sidetrack onto Reports/In-Patient/Symptoms:** added a full-screen scrim (blocking taps and scroll) behind the tour's centered card for informational "Next"-only steps (quick-log, the three trailing nav-tab intros, intro/outro) — steps that don't require touching real content stay fully guided now. Steps that DO require a real tap (Meds tab, Add button, the medication form, Home tab) are deliberately left interactive, unchanged from before. **(7) Tour doesn't return to Home on Finish:** tapping "Finish" on the last step now automatically navigates to Home instead of leaving the user wherever the last step happened to land them; "Skip guide" mid-tour is unaffected. **(8) Replaying the tour forces adding another medication:** added a "Skip this step" control on every wait-required step (Meds tab, Add button, the medication form, Home tab) so any run — first-time or replay — can move past a step that needs a real action without actually performing it. **Also raised by the Owner, not code changes:** whether Dosage options should be numeric-only, and whether one medication should ever carry more than one comma-separated dose (vs. requiring a second medication entry for a different MG at a different time) — both flagged as real product-model questions needing the Owner's direction before any redesign, not implemented blind. **Verification:** self-tested directly (no separate Auditor/Designer/PM chain run this round, matching app-v27's fast-turnaround precedent) — syntax check, then three fresh Playwright suites covering the tour flow end-to-end (background-tap blocking on both step types, Skip-this-step advancing without adding data, Finish auto-routing home, 12 checks), the Dosage options/Daily limit debounce (DOM-node-identity check proving no per-keystroke rebuild, value correctness, eventual live-preview catch-up, the new help icon's content, 11 checks), and the keyboard-focus scroll nudge — plus full re-runs of the app-v27 regression suites (15 checks) to confirm nothing broke. 38 checks total, all passing, zero console errors. Live-verified on the deployed site after clearing the service-worker cache. |
| app-v27 | 2026-07-29 | Owner-reported, medication editor, four items in one fast-turnaround pass (Owner explicitly asked for same-chat fix + push). **(1) Flashing "?" help icon:** root-caused to the medication editor (and its new info-modal) being left out of the app's 1-second tick-render guard — every other bottom-sheet/modal (plans sheet, upgrade sheet, drawer, appointment form) was already excluded from the periodic full-DOM rebuild for exactly this reason, but the editor was missed when that list was built, so the whole sheet — including the small "?" badge next to Schedule windows — was being torn down and recreated every second while open, reading as a flash. Added both to the existing guard. **(2) Clock-style Start time:** replaced the 96-entry, 15-minute-increment Start dropdown on each schedule-window row with a native clock-style time input (`<input type="time">`), so any minute can be set directly instead of scrolling a long list; internal storage (`windows[].start` as a decimal hour) is unchanged, and the picker converts on the way in/out. **(3) Schedule-window name field losing focus every keystroke:** a real, confirmed bug — this field (which defaults to "Daily," the exact field the Owner named) had no stable `id`, so the render()-time focus-restore logic (already used by every other live-typing field in this editor, e.g. Dosage options) couldn't find it again after the full rebuild every keystroke triggers, dropping focus and reading as the box "disappearing" on every type or delete. Fixed with a stable per-row id, same pattern as the other fields — kept as free text rather than converting to a dropdown (the Owner's tentative alternate suggestion), since a window's label is caregiver-chosen free-form text ("With breakfast," "Post-chemo") that a fixed option list couldn't represent. **(4) Onboarding scroll landing below the Medication name field:** could not be reproduced directly in the test environment, but a plausible, well-documented mobile-Safari cause exists (the browser's own toolbar/viewport settle after a tap can silently undo a same-tick `scrollTo`) and the existing navigation-triggered scroll-to-top calls already only run once, synchronously; added a second `requestAnimationFrame`-timed scrollTo after each one as a standard, low-risk defensive fix for that class of issue — no-op on browsers that don't have the quirk. **Also investigated at the Owner's request, no code change:** the "Extra hours between doses (optional)" field on scheduled medications — explained to the Owner as an additional, optional safety lockout layered on top of the reminder windows (blocks logging again within N hours of the last dose even if a window is technically open, protecting against a double-dose if two windows sit close together or an early override was used) rather than a replacement for the window-based schedule; left in place pending the Owner's decision on whether to remove it. **Verification:** self-tested directly (no separate Auditor/Designer/PM chain run this round, given the Owner's explicit same-chat-fix-and-push request) — syntax check, then two fresh Playwright suites covering: the help-icon DOM node surviving multiple tick cycles unchanged; the clock picker accepting an arbitrary non-quarter-hour minute and round-tripping correctly through save/reload; the name field retaining focus and correct value through typing, mid-string deletion, and re-editing (10 checks, all passing); plus a regression pass confirming gap-type ("as needed") medications still save correctly, the periodic tick still resumes normally once the editor closes, and the new info-modal opens/stays open/closes correctly (5 checks, all passing) — zero console errors across both suites. Live-verified on the deployed site after clearing the service-worker cache. |
| app-v26 | 2026-07-29 | Owner feedback on a medication-editor screenshot, five items. **(1) Warning position:** the amber "Dosage options don't include X yet" mismatch warning now renders directly under the Dosage options field instead of after Schedule type, so it appears next to the field it's actually explaining. **(2) Warning contrast:** restyled with a solid amber border, deeper amber text, and a filled "!" badge instead of a plain text line, so it no longer blends into the editor's cream/pink background. **(3) Off-day override for all medications:** medications restricted to specific days (`scheduleDays`) previously showed a dead-end inert card with no way to log on a non-scheduled day — every medication now gets the same arm-then-confirm override used for daily-limit/gap-timer locks, with a "Not scheduled today — Log it anyway?" confirmation, on both the standalone Quick Log cards and the grouped Morning/Afternoon/Evening cards. **(4) Schedule-window timeframe removed:** the Start/End time-range picker (the app-v25 deferred item) is gone — each row is now a single alert time only; End is auto-derived internally (next reminder's start time that day, or midnight for the last one) purely to preserve the existing missed-dose grace-period logic with zero behavior change for existing medications, and is never shown or editable. Because Owner asked to keep that internal end-of-window as the missed-dose cutoff rather than a separate shorter deadline, a new "?" help icon next to "Schedule windows" opens a plain-language explanation of what that cutoff means and when a dose gets flagged missed. **(5) In-Patient tab redesigned:** status card now has a hospital icon badge and a large "Day N" counter with a gradient background while active, plus a note explaining Home logging is paused during a stay; new stats row (Stays / Total days / Longest stay / This year) once any stay exists; History rows redesigned with icon badges, split date/time, and a day-count pill instead of plain text rows; empty state redesigned with an icon. FAQ expanded from 8 to 13 questions to cover the schedule-window cutoff, the new off-day override, Daily limit, the gap timer, and In-Patient tracking. **Chain for this release:** Lead Developer investigation/build (including one Owner-approved architecture decision on the missed-dose grace period, and an Owner follow-up request mid-build for the "?" help tooltip and expanded FAQ) → Zero Day Auditor (found 1 blocker: the off-day override logged doses with no audit-trail flag at all — `confirmTimeAndLog()` checked "early"/"over-limit" overrides but never checked off-day, so a caregiver's deliberate exception was saved indistinguishable from a normal on-schedule dose, a real defect in a medication-safety app; also caught and the Lead Developer fixed live during the same debug pass a copy-precedence bug where a medication that was simultaneously off-day and outside its window showed the generic "opens in Xh" text instead of the correct "not scheduled today" reason; plus 1 should-fix: editing an existing schedule-window row to match another row's time silently dropped the collision at save with no warning; plus 1 nice-to-have hardening item) → Lead Developer fix (off-day now folds into the same `overrideReason` scheme as early/over-limit, supporting all combinations, with full backward compatibility for pre-v26 entries; duplicate window times now block save with an explicit toast instead of silently vanishing) → Designer (conditional pass, required since this release changes visual layout: approved the warning banner and In-Patient redesign outright; found 1 should-fix — the new "?" help icon's tappable area was 20×20px, under this app's own enforced 44px touch-target floor — and 2 nice-to-have polish items) → Lead Developer fix (help icon and info-modal close button both now meet the 44px floor; "1 STAYS" pluralization corrected) → independent PM re-verification of all 7 requested items live plus every fix, GO. |
| app-v25 | 2026-07-28 | Owner-reported, medication editor: the Daily Limit/Limit-unit pair read as two unrelated fields with no indication of what rule they actually produce; the mg-mismatch save error gave no visible reason before Save; "Minimum gap hours" was unclear jargon; general wordsmith pass requested elsewhere on the same screen; the Schedule Type dropdown looked like an old square OS control; and schedule windows use a Start/End time pair when the Owner wants a single alert time. **Fixed and shipped:** Daily limit / Limit unit relabeled with clearer helper text; a new live preview under those fields states the actual rule in plain language ("Blocks logging more once 40 mg is reached today") and, if Dosage options doesn't carry the picked unit yet, shows an amber warning explaining exactly why *before* Save is tapped instead of only failing afterward with no visible cause; the save-blocking toast for that same mismatch reworded with a concrete example; "Minimum gap hours" relabeled to "Hours between doses" (as-needed) / "Extra hours between doses (optional)" (scheduled) with clearer helper text; Schedule Type option copy and the schedule-window "add one" validation toast rewritten in plainer language; the schedule-window preview line corrected from "Reminds between X and Y" to "Reminds at X," matching what the reminder code actually does (fires once, at the start time — never repeatedly through the range) independent of the still-open redesign question below. **Investigated, no code change:** the square-dropdown report was traced to `selectFix()` already applying identical rounded/chevron styling to every select on the screen, confirmed live on the deployed site via `getComputedStyle` — most likely a stale cached build on the Owner's device, not a defect; Owner asked to hard-reload / clear site data to confirm. **Deliberately deferred, pending Owner decision:** replacing the Start/End time-window pair with a single alert time is a real architecture change (the window's `end` value currently also drives the missed-dose deadline and the one-tap-logging open/closed gate, not just a second reminder time) — a migration-safe approach exists (auto-computed hidden grace period, zero behavior change on release for existing medications) but needs the Owner's input on whether/how to expose it before implementation. **Process change (Owner-mandated):** the Project Manager now leads this process — the Lead Developer's role is fixed as implementer only, sitting right after the Zero Day Auditor, with authority to fix and loop-restart but not to declare work finished; PM sign-off now runs as its own independent session rather than a Lead-Developer-authored gate document, closing a self-certification gap present through app-v24. That new PM stage caught a real miss on its first independent run: the "Reminds at X" copy correction above was originally skipped by all three Auditor passes (each treated leaving the line alone as the *correct* outcome of deferring the bigger redesign, conflating the two), and PM sent it back through one more loop before signing off. Chain for this release: Developer investigation → Lead Developer fix → Zero Day Auditor (found 2 issues: the new live preview didn't actually refresh while typing, due to a missing rerender flag; "applications"-unit medications were shown "pill" wording by mistake) → Lead Developer fix → Auditor re-verify (found a new regression the fix introduced: a `type="number"` field's cursor silently reset to position 0 on every keystroke-triggered rerender, scrambling multi-digit entry, e.g. "40" saved as "04" — worse than the original bug since the live preview then confidently confirmed the wrong number) → Lead Developer fix (switched the affected fields to `type="text"`/`inputMode`, which fully supports the cursor-preservation Chrome blocks on number inputs; proactively applied the same fix to two other fields sharing the identical latent bug, never tested by either prior audit) → Auditor re-verify (clean pass) → PM (found the "Reminds at X" gap plus two documentation gaps: this README entry and the Developer brief itself had never been committed) → Lead Developer closes those three gaps → ready for the Owner. All three Auditor passes and the PM pass ran as genuinely independent sessions with zero console errors found at any point across the entire chain. |
| app-v24 | 2026-07-28 | Owner-reported: a Calendar reminder set for "1 hour before" a 10 AM appointment never fired. Root cause: `checkNotifications()` (medication dose reminders) and `checkAppointmentReminders()` (Calendar reminders) both blanket-suppressed every notification on the web build whenever `TEST_MODE` was on — which is always true on this beta build, regardless of whether the Owner had touched the Beta Date Controls at all. Total and permanent, not intermittent, on the exact build the Owner tests with. Fixed so both functions only suppress while the app's simulated "today" is actually offset from real time — a normal session that never opens Beta Date Controls now gets real reminders throughout; suppression during an active date-jump is preserved, since dose entries logged while time-traveled carry simulated timestamps that a real-time check could otherwise misread as "not taken" and fire a spurious notification over. **Two new Calendar reminder options** (Owner-requested, since the old 4 choices never landed exactly where he wanted): "At the scheduled time" and a fully custom, self-set lead time (a number + minutes/hours/days), with full validation (negative values rejected with a toast, 0 accepted as "at the scheduled time"), correct edit/re-arm behavior, and a correct human-readable label in the Calendar list. **Settings → Notifications rewritten** — old copy wrongly claimed reminders "arrive with the phone-app version (in progress)"; now explains reminders fire for real on web but only while the app is open in the foreground (no background/locked-phone delivery without the native app — a real, permanent limitation of the web build's foreground-only tick loop, not something this fix could close, and now stated honestly rather than implied away), plus a new permission-status block (granted/denied/never-answered) with a working "Turn on notifications" action. FAQ copy updated to match. **Process change (Owner-mandated):** TEAM.md's stage order now runs Auditor before Designer (both lanes), and the Full Chain Auditor ("Zero Day Auditor") charter now requires a minimum 20 written test cases per release, covering a full first-run walkthrough, every medication placement/category mode, and a simulated 7-day logging span for every loggable type checked against every display surface — exercised on this very release (45 test cases run, 45 passed). Auditor found 1 should-fix (a pre-existing "zero entries logged anywhere yet" gate was silently blocking a brand-new user's very first dose reminder — its impact was invisible until this release made the pipeline fire for real on web at all — fixed by dropping the now-unjustified clause) and 1 nice-to-have (the "Excluded near treatment day" medication option told users it would be "Hidden," but the medication correctly stays visible as an inert placeholder card by design — fixed the copy to say "Grayed out and can't be logged," matching actual behavior). Designer found 3 should-fix (a "Turn on notifications" button 4px under the app's 44px touch-target floor; a reminder-list bell icon that floated between lines when a long custom label wrapped, a case the old fixed-length options never produced; an FAQ sentence that buried the foreground-only caveat inside an unrelated compound sentence — flagged for extra scrutiny given the real consequence of a caregiver missing it) and 2 nice-to-have (the notifications CTA styled identically to a low-stakes optional button; the "granted" status not reusing the app's existing green success-pill pattern). All 7 findings fixed and re-verified against their specific repro, zero regressions on the full existing verification suite (zero console errors, mobile-first at 390px/360px throughout). |
| app-v23 | 2026-07-28 | Owner's second feature batch, plus two urgent bugs found live-testing folded into the same release. **Scheduled-med gap timers** — a scheduled medication can now optionally also carry a minimum-gap-hours lockout on top of its time windows, the same rule as-needed medications already had; the existing "Log early" override UI picks it up automatically with no separate wiring. **Daily limit reworked** — moved under Dosage options in the editor (was after schedule/gap) with reworded copy explaining what it blocks. **Daily-limit override on Home** — both the standalone Quick Log cards and the grouped Morning/Afternoon/Evening cards can now override a daily-limit block (not just a closed-window block), with a confirmation toast and a new `overrideReason` on the entry so Journal/History badges correctly say "Over limit" instead of always saying "Early." **Select restyling** — all 13 native dropdown call sites app-wide now render with a custom chevron, rounded corners, light background, and the app's own font instead of the dark OS default. **Card text** — Quick Log cards now wrap onto additional lines instead of truncating, "Last dose" copy simplified to "Last taken - Monday 7/27" (Owner's requested format), and the medication's own note (dead code since v12) is visible again. **TEAM.md** gains copy-quality guidance folded into the existing Designer stage, per the Owner's copywriter/wordsmith suggestion, rather than a new 9th stage. **Urgent bug fixed:** adding a brand-new medication immediately flagged 3 "missed doses" for windows that closed before the medication existed — every medication now gets a `createdAt` stamp and `missedDosesFor()` skips any window that closed before it; pre-existing medications are unaffected. **Urgent bug fixed (Owner screenshot):** the phone's system status bar icons were unreadable against the app header — `manifest.webmanifest`'s `theme_color`/`background_color` were stale mismatched leftovers (`#C77800`/`#EEF1F0`) unrelated to the app's actual light-pink header; both corrected to `#FDF1F4` to match, along with the HTML meta theme-color tag. Full chain: Designer found 1 blocker (a long medication name caused horizontal page overflow on the standalone card — fixed by switching 5 button-style variants from no-shrink to wrap-and-shrink flex rules) plus 4 should-fix/nice-to-have (missing explanatory copy and undersized touch targets on grouped-card override buttons, clipped Schedule-type select text, awkward ceiling copy). Auditor found 1 blocker (the grouped card's `courseComplete` lock state — separately from the ceiling-hit case already being fixed — still hit the pre-existing "Opens Invalid Date" bug, a real defect surfaced while building this release, not introduced by it) plus 2 should-fix (override entries always badged "Early" even when the real reason was going over today's limit, now tagged with `overrideReason` and labeled correctly; a strict `<` comparison in `isEarlyAt()` could silently drop the early-override flag entirely for two doses logged in the same clock minute, fixed to `<=`). All findings fixed and re-verified against their specific repro at both 390px and 360px per the Owner's explicit mobile-first testing instruction, zero regressions on the full existing regression suite (zero console errors). |
| app-v22 | 2026-07-28 | Owner-requested: the app "still looks basic" — moved to the standard Android app pattern of a hidden hamburger nav drawer (top-left, three-line icon replaces the old standalone "?" and gear header buttons) opening a slide-in panel with an identity header (active profile name, tap → Settings) and two destinations: **Calendar** and **Settings**. **Calendar** is a new view for tracking future appointments (title, date via the app's existing calendar-grid picker, time, optional note, optional reminder: 1 hour before / morning-of / 1 day before / none) — reminders fire through the app's existing native/web notification path via a new, independent check that can't be taken down by a bug in the medication-reminder system. Upcoming appointments list ascending with a collapsible Past section. **FAQ** — an 8-question accordion added to Settings (privacy, adding meds, switching profiles, missed doses, treatment date, the weight-reason field, Calendar reminders, starting over); medical-advice questions stay explicitly out of scope next to the existing disclaimer. **Replay the walkthrough** confirmed still in Settings from a prior release; the tour's final-step copy updated since it referenced the now-removed header "?" button. **Steroid weight-reason suggestion** (carried over from the app-v21 research report rather than left as a future candidate): the weight-log modal now shows a tap-to-fill hint when an active, non-paused medication name-matches a common oncology steroid (Dexamethasone, Prednisone, etc.) and no reason is picked yet — never auto-selects, just offers. Researched Android nav-drawer conventions (Material Design 3 spec: width, scrim opacity, animation timing, touch targets, focus trap) before building, and flagged to the Owner that current Google guidance actually leans away from combining a drawer with a 5-tab bottom nav on phone screens — built per explicit request anyway, executed to spec. Full chain: Designer found 2 should-fix issues (3 new touch targets under the app's 44px floor; steroid-suggestion and calendar-reminder icons using raw color emoji instead of the app's custom SVG line-icon language) — both fixed. Auditor found 1 blocker (a fast double-tap on the hamburger could land its second tap on the drawer's identity button underneath, force-navigating to Settings — fixed by moving the close button to its own top-right control so the identity button no longer occupies the hamburger's former screen position, re-verified with a real two-click test at the hamburger's exact coordinates), plus 3 should-fix (a "morning-of" reminder for an appointment before 9 AM could fire after the appointment already happened — fixed with an automatic 1-hour-before fallback; editing any field on an already-reminded appointment, even just a note, silently re-armed its reminder — fixed to only reset on an actual date/time/reminder-choice change; nested-button invalid markup on the old close control — fixed as part of the blocker fix) and 1 nice-to-have (opening the drawer mid-tour buried the tour card with no way back — fixed by having the hamburger no-op while the tour is active). All findings re-verified against their specific repro, zero regressions on the full v21 weight/paracentesis regression suite (mobile + desktop, zero console errors both). |
| app-v21 | 2026-07-27 | Owner-requested: document *why* weight changed, not just the number — critical for this patient's Paracentesis fluid drains, which cause a real, fast weight drop that looked alarming in the trend chart with no context attached. Weight logging now has an optional **Reason for change** dropdown (Paracentesis, Fluid retention/swelling, Poor appetite, Nausea/vomiting/diarrhea, Steroid medication, Illness/infection, Eating more, Other), a **Liters drained** field that only appears when Paracentesis is selected, and a free-text **Note**. All three show in the Weight report's reading list (reason + liters under each entry, note italicized beneath) and in the compact History label. New fields (`weightReason`/`litersDrained`/`weightNote`) are additive to the existing weight entry — no migration needed, older entries render exactly as before with the new lines simply omitted. Researched clinically what's worth documenting for chemo/radiation weight changes (ascites/paracentesis nursing guidance, NCI weight-change guidance, cancer cachexia/steroid literature) before building; see Owner report for what else surfaced as future candidates. Full chain: Designer review caught a real bug before ship — the Paracentesis label ("Paracentesis (fluid drained)") hard-clipped mid-character in the reason dropdown on both mobile and desktop with no ellipsis, so once selected the user couldn't reread their own choice — fixed with `text-overflow:ellipsis` on the select. Auditor caught two real defects: liters values outside 0–20 (negative, zero, over 20) were silently dropped at save time with an identical-looking success toast, so a caregiver would see "Paracentesis" logged with no liters and no idea why — fixed to block the save with an explicit "Enter a valid liters amount (0–20), or leave it blank" toast, matching the app's existing validation pattern used for weight/temperature/blood-pressure; separately, `liters === 0` (a legitimate value — "attempted drain, negligible fluid") was being rejected by the same bug even though the field itself allows 0 — fixed alongside. Both fixes re-verified live (rejected: -5, 0-accepted, 25-rejected, blank-accepted; ellipsis rendering confirmed via screenshot) with zero regressions on the full happy-path suite (mobile + desktop, zero console errors). |
| app-v20 | 2026-07-27 | Owner-requested batch of three medication-editor features, shipped together: **(1) Pause a medication** — a manual "Pause"/"Resume" toggle in the editor (edit mode only) to temporarily deactivate any medication for any reason without deleting it. While paused: the medication's card becomes an inert, muted card on Home ("— Paused", no logging), dose logging is fully blocked, missed-dose flags stop accruing, and a daily Home-only check-in banner ("Still pausing {name}?") re-asks once per day until resumed or reconfirmed. Owner explicitly required that resuming must not flood the user with a backlog of "missed dose" flags for the days it was paused — solved by tracking `pausePeriods` (start/end date ranges) rather than gating solely on the live `paused` boolean, so `missedDosesFor()` permanently skips exactly the days a medication was actually paused, whether or not it's still paused now; verified live across real simulated day boundaries (pause → 2 days pass → resume) with zero missed-dose flags appearing for the paused window. **(2) Excluded near treatment day** — the inverse of the existing v16 "Only near treatment day" feature (a medication hidden/unavailable during a configurable window around the treatment date, instead of the reverse). The old single boolean toggle was replaced by a 3-way "Treatment-day availability" radiogroup (Always available / Only near treatment day / Excluded near treatment day), reusing the existing per-medication days-before/after fields so both modes share one exclusive control instead of two independent toggles that could contradict each other. **(3) Schedule-window time picker** — the free-text "Schedule windows" field (e.g. "8 AM-12 PM Morning", prone to silent typos that dropped a window entirely) was replaced with a repeatable list of Start/End-time dropdown rows at 15-minute increments plus an optional per-row label and a live plain-language preview ("Reminds between 8:00 AM and 8:30 AM"); window times now store as decimal hours (`8.25` = 8:15 AM) instead of whole hours only, fixing several landmines found during investigation (`hourTs()` truncated fractional hours, `dueRemindersAt()`'s reminder-firing check only compared whole hours, `normalizeMedication()`'s start-time clamp capped at 23 instead of 23.75) that would otherwise have silently broken quarter-hour reminders. Quality Chain: Designer review found 4 issues (two 40px touch targets under the app's 44px convention, two badges for opposite treatment-day states sharing one color, a paused row in grouped Morning/Afternoon/Evening cards missing the muted styling its standalone counterpart got) — all fixed directly per the Owner's minor-fix amendment. Lead Designer re-verification caught a real regression the touch-target fix itself introduced (the enlarged delete button no longer fit on the same line as the two time selects at 360-390px, silently wrapping instead of failing loudly) — fixed by trimming the row's gap/select minimums to restore the deleted 4px plus margin, confirmed live at both widths with 2+ windows. QA Tester found and got fixed a toast-occlusion bug (the save-confirmation toast blocked taps on the medication editor's Discard/Add buttons at keyboard-open phone heights) — fixed with `pointerEvents:'none'` on the toast, since it never needed to receive a tap. **Chain restart:** the Auditor's first full pass then found two MAJOR (P1) defects that sent this release back through the whole chain per `TEAM.md`'s restart rule: (1) deleting a medication and re-adding one under the same name silently discarded its `pausePeriods`, reviving exactly the "missed dose flood" the pause feature was built to prevent — fixed by archiving `pausePeriods` on delete (closing any still-open period at that moment) and restoring them when a new medication's derived id matches an archived entry, which also surfaced and fixed a second, independent bug where `loadMedicationConfig()` silently discarded the just-archived data on reload whenever the medication list was validly empty; (2) the standalone Home card and the grouped-card row disagreed on whether "paused" or "excluded near treatment day" won when a medication was both — the standalone card vanished entirely instead of showing "Paused" — fixed by moving the exclusion check into the card's per-row rendering so pause always wins, matching every other guard in the file. A fresh Designer/Lead Designer/QA/Auditor pass on the restart caught a third variant of the same ordering bug for the sibling "only near treatment day" mode (paused + only-mode-outside-window also vanished) — fixed with the same pattern. Full chain re-ran clean end to end; Lead Auditor independently reproduced the core scenarios via real UI interaction (not just state injection) before clearing it to the Project Manager gate. One pre-existing, unrelated, non-blocking issue surfaced by QA during the restart pass (the History day-summary count includes non-dose entries like vitals/symptoms) is logged for a future pass, not fixed here. |
| app-v19 | 2026-07-26 | Owner-reported: Zofran/Iron/Compazine cards stayed "Available" right after logging a dose, even though the dose showed up correctly in Today's journal. Root cause confirmed by Aaron checking his own device (no cloud sync, so this couldn't be verified remotely): those medications are "As needed" (`type: 'gap'`) with **Minimum gap hours left at 0** — by the existing design, a 0/blank gap means no cooldown at all, so the card re-opens the instant it's logged. That's not what any real medication needs. Fix (Owner-specified): `saveMedicationEditor()` now rejects saving a gap-timer medication with 0 hours (`type==='gap' && gapH<=0` → toast, save blocked), the "Minimum gap hours" field copy changed from "Leave blank for no minimum gap" to "Required — how long before this can be logged again," and the Meds-tab rule summary for any medication saved before this rule existed now reads "No minimum gap set — edit to fix" instead of "As needed," so Aaron can spot and correct Zofran/Iron/Compazine (and any other pre-existing zero-gap medication) at a glance. Scope: HTML beta only (APP-BETA), per Owner instruction — no native/Capacitor-specific code touched. Existing medications are unaffected until edited (no silent data migration); this only gates future saves. QA: Node syntax check clean; verified new save-blocked toast fires for gap=0/blank and clears once a positive value is entered; verified `formatRuleSummary` warning text renders only for `type:'gap'` meds with `gapH<=0` and leaves scheduled/window meds and meds with a real gap value untouched; verified the Home card lock/unlock math itself (`status()`) was not touched — this is purely a data-entry guardrail, not a change to how gap timers are evaluated. |
| app-v18 | 2026-07-26 | LEAN RUN per TEAM.md Owner amendment ("if there is an issue...it needs to be fixed. just tell me what was fixed afterwards"). Fixes the Reports → Blood Pressure crash QA found (and flagged as pre-existing, out of scope) during the app-v17 walkthrough: `renderBloodPressureReport()` returned a single DOM node in both its empty-state and populated-state branches, unlike every sibling report renderer (`renderHistory`, `renderWeightTrend`, `renderCycle`, `renderAppetite`, `renderBowelMovementReport`, `renderInPatient`), which all return an array. `renderReportDetail()` spreads whichever renderer's result it gets (`...content`) into its own returned array — spreading a bare `Element` isn't valid, so it threw `TypeError: content is not iterable` the instant the tile was tapped, before the new DOM ever replaced the old one, so the tap produced zero visible feedback and the same error then repeated once a second forever via the app's existing 1s tick loop, until the user navigated away. Pure display bug: `logBloodPressure()`/`addEntryDB()` write straight to storage on a code path this bug never touched, so no reading was ever at risk — readings just couldn't be viewed. Fix: wrap both `return` statements in `[...]`, matching the sibling convention exactly; nothing else in the function changed. Combined verification pass (Designer + QA + Auditor in one lean pass, PM checklist run by the Lead Developer, per TEAM.md's lean-mode provision) confirmed: empty state renders with zero console errors across 3+ tick cycles; a real logged reading renders correctly and the Home card's "Last sys/dia" stays in sync; 3 readings render together correctly, sorted newest-first, verified against raw localStorage, not just on-screen text; Remove works mid-list and also removing-to-empty falls back to the empty-state copy cleanly; all 6 report types (including the preference-gated Cycle report) still open with zero regressions; the Home blood-pressure quick-log card, a fully separate code path, is unaffected. Verified at 360×740 and 390×844. One pre-existing, unrelated, non-blocking observation surfaced and logged for the backlog: BP entries logged within the same second can share a truncated timestamp, affecting only the display order of same-second ties, never data loss — not touched by this fix. |
| app-v17 | 2026-07-26 | Owner-reported: the guided-tour banner and its highlighted target blended into the app's own rose accent family and didn't grab attention. Both now get a green (`#2E7D4F`, the app's existing "success/active" token — Plans sheet "Current plan" border, Settings "Active" badge) 3px border/outline with a pulsing glow, replacing the old translucent rose. Pulse uses a phase-synced negative `animation-delay` computed from `Date.now()` on every apply (`tourPulseDelay()`/`applyTourHighlight()`) so a freshly-rebuilt DOM node (the whole app re-renders every 1s tick) resumes the glow mid-cycle instead of restarting it — the same flicker class app-v11 fixed for the Plans sheet, but for a continuous loop instead of a one-shot animation. Design review caught and this pass fixed two real issues before ship: the pulse's contrast floor (0.3 alpha) fell under the WCAG 3:1 minimum against white/card backgrounds for roughly half of every cycle — raised to 0.75; and `prefers-reduced-motion` didn't actually stop the glow — the app's existing global CSS guard neutralizes one-shot animations but not `infinite`-iteration ones, so it was looping at an absurd frequency instead of freezing, a real flicker risk for photosensitive users. Fixed with a JS-level `prefersReducedMotion()` check that skips the animation properties entirely when set, rather than relying on the CSS override. Full chain: Designer + Lead Designer (independent re-verification of both fixes), QA "User Zero" full first-run + daily-loop walkthrough (tour change itself: clean pass, including rapid-tap and double-tap-through-highlight edge cases), Auditor (code audit + 7 live edge-case journeys: double-tap tour restart, navigate-away-mid-tour, tour step-back on discarded med editor, reload-mid-tour, live reduced-motion toggle, viewport resize, missing-target guard — all pass). QA independently found an unrelated, pre-existing bug while exercising the whole product (Reports → Blood Pressure crashes on open, `renderBloodPressureReport()` returns a single node instead of an array like every sibling report) — not part of this release's diff, tracked separately, not fixed here. |
| app-v16 | 2026-07-25 | Two Owner-reported bugs fixed, both real (not UX confusion) — full chain incl. independent Auditor agent. (1) **Custom per-medication treatment-day window**: the "Chemo-day only" toggle previously hid/showed a medication using a hardcoded fixed ±1-day window around the treatment date, inherited from the original single-patient Dexamethasone regimen — inescapable for every medication regardless of that patient's actual regimen. Renamed to "Treatment-day medication" with two new editable fields, Days-before/Days-after treatment (e.g. Aaron's example: 3 days before, 3 days after), defaulting to 1/1 so existing configs behave identically until edited (`treatmentOnly`/`treatmentDaysBefore`/`treatmentDaysAfter` replace `chemoOnly`, migrated in `normalizeMedication`). All "Chemo" wording on the toggle, its badge, and the global treatment-date card/toasts is now "Treatment," matching language already used elsewhere in the app (Home's "Treatment schedule" card, "Treatment day" banner). (2) **Dose reminders now actually work for real medications**: `checkNotifications()` had always fired on a hardcoded clock schedule for five hardcoded legacy medication ids (protonix/iron/compazine/buspirone/paroxetine) — ids `RESERVED_LEGACY_MED_IDS` explicitly forbids any user-created medication from ever having, so every reminder branch was unreachable dead code on every real install; there was no way to get a reminder for a medication actually added to the app. Replaced with a generic `dueRemindersAt()` that reads the same data every scheduled medication already has (its own `windows` + auto-derived `alerts` flag) — the same source of truth the passive missed-dose banner already used — firing once per schedule window per day if the dose isn't logged yet. Independent Auditor agent caught and this pass fixed two real bugs before ship: `clampTreatmentDays('')` was silently saving 0 instead of the documented fallback of 1 (a cleared day-count field would silently narrow a patient's window), and the reminder de-dupe key collided when a medication had two schedule windows starting at the same hour (second window's reminder would never fire). QA: Node harness 35/35 (30 pure-logic incl. DST spring-forward + both audit-caught edge cases, 5 migration-safety), Playwright smoke 360/390/1280px zero console/page errors, full tab sweep clean, `app-v16` confirmed in Settings. |
| app-v15 | 2026-07-24 | Safe-area fix (Aaron's APK bug report): header, guide banner, tour-card clamp, welcome screen, and time-modal overlay now clear the system status bar via a single --safe-top CSS var — env(safe-area-inset-top) when the platform reports it, a 28px literal only when a WebView reports 0 (probe + '; wv)'/isNativeApp detection; replace-not-sum, so no double padding). Zero layout change in desktop browsers. Quality Chain: LEAN RUN per Owner amendment (Developer brief + combined design/QA/audit verification + PM checklist by Lead Dev). |
| app-v14 | 2026-07-24 | Native Android test shell (Owner: "Maximum Effort" — full chain, not lean). Adds a Capacitor project (`android/`, `capacitor.config.ts`, `www/` fallback splash) built for on-device testing of real look/feel + real notifications before anything goes near a store. Deliberately scoped to shell + notifications only — SQLite storage migration and real store billing stay queued (not needed to test feel/notifications, avoided rushing them). Runs in **remote mode**: the native shell loads the live GitHub Pages URL (`server.url` in config) rather than bundling a local copy, so the existing push-via-Chrome workflow keeps updating the app instantly with zero rebuild for content/UI changes — only native-only code (this config, `android/`, the notification bridge below) ever needs a rebuild. New `isNativeApp()` helper (checks `window.Capacitor.isNativePlatform()`, wrapped in try/catch) gates every native-only path so **web/PWA behavior is byte-for-byte unchanged** — proved by re-running the full v14 regression suite with the Capacitor bridge scripts unreachable (this sandbox's network blocks all CDN hosts) and confirming zero errors and identical behavior. Notification system rewired: `checkNotifications()` now fires for real inside the native build even under `TEST_MODE` (the whole point of this test build), `sendNotif()` branches to `Capacitor.Plugins.LocalNotifications.schedule()` natively instead of the browser Notification API, and a genuinely dormant pre-existing bug was fixed along the way — `requestNotifPermission()` was defined but never once called anywhere in the app, so even a real production web user would never have been prompted for notification permission; it's now wired into startup via `subscribePush()`. Settings' Notifications copy is now conditional on `isNativeApp()`. App icon generated from the real ChemoWell mark at all 5 Android densities (legacy + adaptive foreground/background) plus a proper white-silhouette status-bar notification icon extracted from the mark's cross shape. Build itself runs in GitHub Actions (`.github/workflows/android-build.yml`), not this sandbox — the sandbox's network allowlist blocks Google's Android Maven/Gradle Plugin Portal entirely, confirmed via direct request (403s), so CI is the real build environment for anything native going forward, not a convenience. Debug-signed APK only; a release keystore and store-track billing/SQLite are separate future chain items. QA: full v14 regression suite (8/8) — zero console/page errors across load + 3+ notification-check tick cycles, no horizontal overflow, version string confirmed, Meds/Settings regression-clean. Known gap: on-device notification firing and native look/feel cannot be verified from this sandbox (no Android device/emulator here) — verified via code audit + CI build success only; Owner has the device-testing checklist for the parts that require a real phone. |
| app-v13 | 2026-07-24 | Med-editor merges (Owner-approved R1/R5/R6; LEAN chain run per TEAM.md Owner amendment — Dev brief + Lead Dev + one combined verification pass): the four additive Home-placement toggles are now ONE single-choice "Home screen placement" picker (Own card / Morning / Afternoon / Evening group / No Home card) whose value derives from the same four stored booleans — no schema change, save path untouched; legacy multi-group combos surface as a conditional "Custom (current mix)" option that preserves the exact stored layout so an unrelated edit never rewrites a patient's Home. The two overlapping "as needed" options are unified by dynamic labels: Schedule type reads "As needed — gap timer between doses" / "Scheduled — set time windows"; the Days-taken as-needed option reads "No set days" for gap meds and "As needed — don't flag missed doses" for scheduled meds (its real safety meaning), with a contextual helper line. "Frequency / rule note" → "Notes" with a placeholder that no longer coaches duplicating enforced rules. QA: round-trip invariant verified byte-idempotent by real clicks (canonical, multi-group, win+asneeded), alerts safety derivation confirmed untouched, v12 first-run suite re-run clean. |
| app-v12 | 2026-07-24 | MOBILE-FIRST onboarding fix (Owner-reported) + units-display blocker (caught by the new QA "User Zero" role, added to the chain this release along with the binding mobile-first testing rule — see TEAM.md). (1) Tour occlusion: on every action step (Meds tab, Add, med form, Home) the big guide card — which buried the form and Save on phones — is now a slim banner pinned over the header brand row; "More" expands the full card (gains "Got it" collapse), Skip always visible; discarding the editor mid-step OR navigating away via the bottom nav steps the tour BACK instead of stranding it (audit P3-1); real-tap verified at 390x844 and 360x400 keyboard height (25/25 checks). (2) Gap-hours is a grey placeholder 0 (type directly; empty saves as As-needed) with new helper copy. (3) Redundancy cuts: duplicate editor Cancel, repeated header sub-copy, stale Settings breadcrumb, two dead display:none spans per med card. (4) UNITS BLOCKER (HIGH): switching °F/°C (lbs/kg) relabeled stored readings without converting — 99.1°F showed as "99.1°C" in fever red. Fix: readings stamp their unit at log time (legacy entries inferred from dose string), ALL displays convert to the current unit (Home, journal, history, weight chart/stats/hub), fever/high styling compares the displayed value in the display unit (38.0°C≡100.4°F exact), stored values never rewritten (exact round-trip, no drift); unit-aware temp input bounds; Settings caption updated. (5) Design-system conformance from chain reviews: banner label 12px, all tour buttons + Add pill + editor save 44px, editor title TYPE.title. QA: units suite 14/14, first-run suite 25/25, v11 regression suite re-run clean, QA User Zero full fresh-user walkthrough (91 screenshots) — blocker re-verified RESOLVED clicks-first. Queued (pre-existing): tour replay flow (D1-D4), toast z-index/pointer-events, sheet keyboard a11y, onboarding missed-dose grace, Waiting-after-complete label, check-in first-day feedback, report Back-pill overlap, weight-chart duplicate x-labels. |
| app-v11 | 2026-07-24 | First release through the full Quality Chain (TEAM.md). Fixes both Owner-reported phone bugs: (1) Plans bottom sheet no longer flickers — the 1s tick loop now skips re-render while the sheet is open (upgradeOpen joins timeModal in the tick guard), the sheetUp entry animation plays once per open via a module-level animate-once flag (reset on close, so reopening animates), and the sheet's internal scroll survives interaction re-renders (id-requery scrollTop preservation in render(), clamps when content shrinks after purchase). (2) Pull-to-refresh no longer dumps you to Home — active tab persists in sessionStorage (strict 6-view whitelist, falls back to Home on garbage; reportsView/modals/drafts deliberately NOT persisted; factory reset clears it so a wiped app boots to welcome→Home; profile switch now lands back on Settings), plus html/body overscroll-behavior-y:none suppresses the browser refresh gesture on Android (iOS best-effort — tab restore is the guarantee there). Audit P3-3: eraseAllAppData references UI_VIEW_KEY. Chain artifacts in outputs/: DEV_BRIEF_v11, DESIGNER_REVIEW_v11 (PASS x5), LEAD_DESIGNER_SIGNOFF_v11 (PASS), AUDIT_v11 (0 P0/P1; P2 README row fixed; P3-1/2 pre-existing and P3-4 new-in-v11 cosmetic — all queued). QA: 12/12 fix-specific checks, 60+ audit checks, 15 journeys, zero console errors. Known queued (pre-existing): toast hidden behind sheet scrim (z50<z70), sheet keyboard a11y (focus/Escape), double-tap-scrim insta-dismiss. |
| app-v10 | 2026-07-24 | Design batches 2+3 (per DESIGN_SPEC_B23, 24 items): 7-step TYPE scale (12px text floor, weight 800 reserved for display/CTAs); header rebuilt as a two-row app bar (44px ?/gear buttons, calm amber BETA badge, redundant orange strip deleted, no clock — title + date only per Aaron); bottom nav 56px items with active dot + rose tint, aria-current='false' bug fixed; all nine alert banners unified to one calm notice recipe (white card, 4px tone accent, tinted icon chip, sentence-case ink titles — no red floods); chemo banner reframed supportive ('Treatment day'/'Recovery day'); NEW dose-progress ring in header (44px SVG, taken/scheduled of today's window-med doses, green ✓ when complete, hidden when nothing scheduled — new pure helper doseProgressToday); paywall converted to bottom sheet (drag handle, sheetUp animation + reduced-motion guard, scrim tap-close, Plus featured w/ Most-popular badge + gradient CTA, TEST_MODE handlers untouched); Quick Log dose buttons and ~20 other touch targets raised to 44px; a11y sweep (8 contrast fixes, 12px floor, keyboard access on clickable divs, aria-labels on icon buttons, dialog/toast ARIA, :focus-visible rule, aria-pressed on segmented toggles, meter text alternatives); time-modal dark-theme leftovers fixed; favicon link added (kills 404); empty-journal copy nit fixed. QA: node --check clean, doseProgressToday harness 5/5, Playwright smoke on 360/390/1280px — every tab, fresh-install setup, tour skip, med add, simulated purchase, ring 0/2→1/2→✓ — zero console/page errors, zero horizontal overflow. |
| app-v9 | 2026-07-23 | Design batch 1 (per DESIGN_SPEC): warm ivory canvas with pink band only at top, solid white cards with hairline #EBE3E4 borders (glassmorphism fully removed), deeper AA-compliant rose #A24C71/#8E3D61, darker ink text, gel inset button shadows replaced with soft drops, decorative background blobs deleted. Product fixes: 'On device' sync-era indicator removed; Treatment schedule card now says 'No chemo/radiation date set'; Days taken gains 'As needed (no set days)' — available any day, never flagged missed. Also updated version string to app-v9. |
| app-v8c | 2026-07-23 | Neutral welcome placeholder (no suggested name). Check-in dates rendered as bold highlighted chips so the day jumps out. Bowel Movement gains a "Loose" option (distinct from Diarrhea). New Blood Pressure tracking: Home card (sys/dia, validated, toggleable in Settings) + Blood Pressure report with full history and remove. |
| app-v8 | 2026-07-23 | Audit burn-down (P1/P2): new Settings screen behind header gear — Profiles (now with delete + confirm), plan/upgrades, Home screen customizer (temp/weight/treatment cards toggleable; daily weight / bowel / appetite check-ins now OPT-IN, cycle tracking opt-in), units (°F/°C, lbs/kg), replay guide, About + medical disclaimer, Start over (moved from Meds). Check-ins never escalate red on install day. Zero-med Home shows an Add-first-medication CTA. Tour progress persists across restarts + Back button. Take-all re-validates locks at confirm time; daily-limit unit mismatch blocked in editor. The original patient's regimen data (DEFAULT_MEDS/seedDemo/senokot migration/protonix copy) deleted from bundle. DST-safe day math (hourTs/nextDay). Stale-tab profile-switch guard. Google Fonts removed (system fonts — no IP leak), viewport-fit=cover. 'Chemo schedule' → 'Treatment schedule'. QA: 35/35 harness checks. Hotfix v8b: the seedDemo removal left a dangling `async` keyword that crashed the whole app at load (caught by live smoke test minutes after deploy; node --check passes on a bare identifier so only runtime verification caught it). |
| app-v7 | 2026-07-23 | Audit P0 burn-down: (1) missed-dose tracking now works for user-created scheduled meds (alerts auto-set for window meds); (2) user meds can never take legacy ids (Zofran/Tylenol/etc.) so no one inherits the original patient's lockouts or the silent 2,500 mg APAP ceiling — all CONFIG.ceilingMg fallbacks removed; (3) chemo banner genericized — no more prescribed premeds copy, legacy chips only when those meds exist; (4) Bowel/Appetite yesterday check-ins gated on install date; (5) Erase-all preserves purchases; (6) cloud-era ghost copy purged (check-connection toasts, Connecting…, a patient-name placeholder, 156.0, live-sync text); (7) schedule windows auto-sorted (protects missed-dose claiming). QA: 24/24 harness checks. |
| app-v6 | 2026-07-23 | Per-medication day scheduling: new "Days taken" in the med editor — Every day / specific weekdays (7 day-pill toggles) / every N days from a start date. Off-days: quick log + grouped cards show "Not scheduled today," missed-dose logic and grouped Take-all skip them. New "Group with afternoon meds" toggle + Afternoon meds Home card. Schedule badge shown in med manager. QA: 28/28 harness checks. Also fixed a renderer-wide bug found during QA: h() set `value` only as an attribute, so every re-rendered <select> silently displayed its first option instead of the selected one — value is now applied as a property after options mount. |
| app-v5 | 2026-07-23 | Multi-profile + plans: profile-namespaced storage (auto-migrates v1-v4 data into profile p1), Profiles section on Meds tab (list/switch/add), Free (1 profile) / Plus $4.99 (3 profiles + export/backup coming) / Pro $14.99 (unlimited) one-time tiers, plans modal with TEST_MODE simulated purchases + Reset-to-Free for beta testing. Erase-all now wipes every profile + license. QA: 21/21 harness checks (migration, tier gating, switch/reload, downgrade keeps data). |
| app-v4 | 2026-07-23 | "Start over" section at the bottom of the Meds tab: Erase all data… → confirm step → wipes patient name, medication list, and all logged history from the device and reloads to the welcome screen + guide (true factory reset). |
| app-v3 | 2026-07-23 | True from-scratch start: fresh installs get an EMPTY medication list (the original patient's defaults removed; no auto-seeding). New interactive first-run walkthrough: 10-step coach-mark guide that highlights real UI elements and waits for the user's own taps — Meds tab → Add → fill out the form → save first med → back Home → tour of dose logging, Reports, In-Patient, Symptoms. Skippable, never re-nags after skip/finish (prefs.tourDone), relaunchable via new ? button in header. QA: 27/27 harness checks. |
| app-v2 | 2026-07-23 | Fresh-install fix: missed-dose tracking now starts at `installedAt` (stamped into prefs on first run) instead of a hardcoded past date — a new install no longer shows dozens of phantom "missed doses from previous days". |
| app-v1 | 2026-07-23 | Seeded from chemowell-beta v71. Firebase/Firestore/FCM fully removed → on-device storage layer (entries + prefs in localStorage, same function names: subscribeEntries/addEntryDB/removeEntryDB/subscribePrefs/setPrefsDB). First-run welcome screen asks patient name (stored in prefs; no hardcoded name). "Live sync" indicator → "On device". APP BETA badge/banner. SW cache chemowell-app-v1. Storage layer QA: 10/10 Node harness checks. |

### app-v68 — the render scan, and the two things it found on its first run

`test/overflow-scan.mjs` walks every screen at eight phone widths (four iPhone, four Android),
opens the medication editor, and fails the run if anything spills its box, if a screen cannot be
reached, or if the app logs a single console error. It is the first check in this app that looks
at the screen rather than at data. It found two real defects immediately:

- **Every saved medication silently disappeared on load.** A `const` used by the treatment-window
  clamp sat in the temporal dead zone during module start-up, where `loadMedicationConfig()` runs.
  It threw, the surrounding `try/catch` swallowed it, and the app fell back to an empty medication
  list — no crash, no message, nothing on screen to explain it. All four unit suites stayed green,
  because they lift functions into a sandbox and never run module start-up. Fixed by making the
  bound a hoisted function.
- **The app did not fit a 320px or 360px phone.** The "Days taken" picker sizes itself to its
  longest option ("Every few days (for example, every other day)"), and `min-width: auto` means a
  `<select>` will not shrink below that. It forced the whole document to a 379px minimum, so on an
  iPhone SE/mini and on 360px — the most common Android width in the world — the entire app scrolled
  sideways and text ran past the edge of the screen. This is the bug Aaron reported. Fixed with
  `max-width:100%; min-width:0` on every `<select>`, plus a nav label that scales with the screen.

  **The nav label took three attempts and the first two were wrong the same way.** A single step at
  360px, then a two-step (tighten at 360, shrink at 320) — and that second one left 321–345px with
  neither treatment, so the longest label spilled into its neighbour right through that band. The
  render scan passed it because its width list jumped 320 → 360 and never looked between the two
  breakpoints the release had just created. A breakpoint is a width nobody tested. The label now
  scales continuously with the screen instead, and the scan samples 330 and 345 as well — ten
  widths, not eight. Swept at every whole pixel from 300 to 500 by the Zero Day Auditor: everything
  fits from 308px up, tightest at 325px with 3.8px of slack.

The scan's own blind spot is worth recording: under mobile emulation a browser WIDENS the layout
viewport when content refuses to fit, so every element then "fits" the now-wider page and a
per-element scan reports clean. The first version did exactly that. It now measures the page itself
against the device width before measuring anything inside it.

#### Correction to the app-v68 notes above

Two claims in the app-v68 entry were overstated, and the Zero Day Auditor caught both. They are
corrected here rather than quietly edited out, because this same entry already carries two earlier
corrections and the pattern is the point.

- **"A window typed as text silently collapsed back to 1/1" did not happen in any shipped build.**
  `saveMedicationEditor` has clamped that field to a real number since app-v16, the release that
  introduced the feature (`git log -S` confirms it). Nothing the medication editor saves has ever
  been a string. The `normalizeMedication` fix is still right — it is the one place every saved
  medication passes through, and a restored backup or an imported file can hand it a string — but it
  closed a hole, it did not fix a bug users were hitting.
- **The "badge says −300/+300 while the app obeys 14" divergence never reached a released build
  either.** It was created and removed inside this same delta. Before it, there was no upper bound
  anywhere at all.

The unbounded window itself was real: a mistyped 30 would have made a medication treatment-adjacent
for two months either side, and nothing stopped it.

### The app and its instructions disagreed about treatment dates

Found by the seventh Zero Day Audit and confirmed **live in published app-v67**, so it predates the
v68 work. Help said *"Setting a new date replaces the old one."* The app **adds** it.

Correct a mistyped 20 Aug to 24 Aug and both dates stay in the schedule: Zofran still blocks on the
21st and 22nd, Dexamethasone is still expected on the 19th through the 21st. Someone following the
printed instruction believes they have fixed a mistake that is still driving their medication rules.

**The behaviour is right and was left alone.** A course has several treatments and every one of them
should count — that is what app-v68's "ask every treatment date" change was for. The defect was the
app describing itself wrongly, so the words were fixed, not the logic: Help now says a date is
**added**, warns that a mistyped one keeps working until it is dealt with, and states that Clear
removes **every** date rather than just the wrong one.

**Still open, and the reason this is only half a fix:** there is no way to remove ONE date. Clearing
is all-or-nothing, so correcting a typo means wiping the schedule and re-entering it. That is a real
change to treatment-date handling — the logic that drives every medication rule — and it needs its
own release rather than being folded into a wording fix.

**Gate:** `test/v69-treatment-date-help.mjs` — 9/9, pinning the behaviour and the Help text
*together*, because either drifting recreates the defect. Falsified both ways: restoring the false
sentence → 7/9; making the app genuinely replace → 7/9 on the other two checks.

One note on the suite itself: its first locator searched for `"treat-clear"` and found that string
inside **another topic's `related` list**, extracting the wrong 50 characters and reporting four
failures against an app that was already correct. Anchored to `{ id: "treat-clear"`. The same shape
as this project's `keywords: [` incident, where a literal in ordinary prose fooled a gate.

### A hospital stay made the whole app scroll sideways

The PM found "Home is 4px too wide at 320px during a hospital stay" by hand. Putting the render
scan into that state found **two** defects, one of them on eight of the ten widths.

- **The page itself was wider than the phone.** The In-Patient banner's **Log In-Patient End**
  button sat in a non-wrapping row as a fixed-width item beside a flexible one, so it forced the
  document to 324px on a 320px screen. The entire app scrolled sideways because one button would
  not give way. The row wraps now.
- **The stay statistics spilled their tiles** at every width from 320 to 390. Four tiles across a
  320px phone leaves each one roughly 34px of usable width while the word TOTAL needs about 36px.
  They are two by two now, sized from the longest label rather than from how many look tidy.
  *(Correction, from the Zero Day Auditor: those two pixel figures are my own arithmetic off the
  box model and the auditor could not reproduce them independently. The overflow itself is not in
  doubt — it is reproduced by the scan at eight widths and disappears with the fix — but treat the
  exact numbers as the reasoning that picked the layout, not as measurements.)*

**Neither was visible to a scan that had walked In-Patient ten times and called it clean**, because
it had never once opened the app *with a stay in progress* — so that banner had never been drawn
under test. Seeding one immediately exposed a third thing: the scan's own In-Patient needle was
pinned to "Log In-Patient Start", which does not exist while a stay is open, so the screen went
unreachable at every width. The needle only ever held because the app had only ever been tested in
one of its two states.

**On the falsification, because it took four attempts and the reason is worth keeping.** Reverting
the button change alone left the scan clean. Reverting the row's `flexWrap` alone also left it
clean. Only reverting **both together** turned it red — the two changes were each independently
sufficient, so neither could be falsified on its own. A fix nobody can falsify is a fix nobody can
safely remove later, so the button was returned to exactly how it was and the wrap kept as the
single load-bearing change, which *is* provable alone.

*Correction, from the Zero Day Auditor: that "one load-bearing change" rule describes the **banner
row** only, and this section previously read as though it covered the whole commit. The **stat
tiles** carry two changes — `flexWrap: 'wrap'` on the row and a `calc(50% - 4px)` basis on the tile
— and that is fine, because unlike the banner pair neither is sufficient alone: the auditor
reverted each separately and each one goes red on its own. The rule is "no redundant fixes", not
"no more than one change".*

That is a distinct failure from the two already recorded this week. A sabotage that does not apply
gives a green that proves nothing; a sabotage that fires the wrong check gives a red that proves the
wrong thing; **two redundant fixes give a green that proves neither.** All three look like evidence.

**Gate:** `test/overflow-scan.mjs` — 140 of 140 combinations, 0 overflowing, with an open hospital
stay in the fixture. Falsified: both levers reverted → the page-width finding returns; four-across
tiles → the label overflow returns at the same pixel counts.
